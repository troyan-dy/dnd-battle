# STATE.md — Handoff Log

> The orchestrator reads this FIRST each cycle, then ROADMAP.md.
> Each agent appends a short entry. Keep it terse. Newest at the top.

## CURRENT FOCUS
Bugfix cycle (first manual end-to-end run by host surfaced two breakages):
(1) **SPA/API route collision** — the host "Configure characters" link pointed at `/rooms/:id/characters`, which is ALSO a real backend endpoint (`GET /rooms/{id}/characters`); nginx (`location /rooms`) proxies all `/rooms/*` to the backend, so the browser got raw JSON (`[]`) instead of the config SPA page. FIX (frontend-only): moved the SPA config page OFF the API namespace to `/host/:roomId/characters` (`router.tsx` regex, `CreateRoomScreen` link, App/router tests updated). The backend keeps full ownership of `/rooms/*`; no backend/nginx change needed.
(2) **No map-upload UI** — the host invite link opens the board, but with no map and no way to add one it looked empty. The backend already had `POST /rooms/{id}/map`; the frontend never called it. FIX: added `uploadMap()` to the API client (multipart, bypasses the JSON-header `request()` helper) + `MapResponse` type; MapBoard now shows a host-only "Upload a map" affordance in the no-map overlay and a "Replace map" button in the grid controls, cache-busting the map URL via a `mapVersion` bump on success (players see a "ask your DM" hint instead). Unit tests added for host-upload + player-no-upload.
(3) **Added characters never appeared on the board** — the board renders *tokens*, not bare characters, and `add_player` only creates a Character/Participant/InviteLink, never a Token (placement is the separate `POST /rooms/{id}/tokens`, which NOTHING in the frontend called). FIX: did NOT auto-create the token in `add_player` (that would make `place_token` dead — a character would always already have one → 409 — and break the e2e token seeding); instead the frontend `CharacterConfigScreen` now, right after `addPlayer`, calls `listTokens` + `placeToken` to drop the character's token, cascaded across a 20-wide band (`x = n % 20`, `y = n // 20`) so they don't all stack on (0,0). Placement is best-effort: on failure the character + invite link still exist and a non-fatal warning is shown. Tests added (cascade placement + warning path). Backend untouched → backend suite stays green (45 passed in the two affected files).
Verified GREEN: tsc clean, oxlint clean, vitest 191 passed; backend rooms/tokens suites 45 passed. NOTE: e2e (Playwright) not run this cycle. Map upload AND token placement are still UNGUARDED server-side (same Backlog auth item as the other host-config endpoints). Known limitation: REST placement does NOT broadcast, so a board already open in another tab won't show the new token until reload (same as map upload).

### Previous focus (Phase 7 — DONE)
Phase 7 COMPLETE — all roadmap phases (0–7) are now [x]. Final task "e2e Playwright: two browser clients, one moves → other sees it" DONE this cycle. The prior infra blocker is RESOLVED: the "no network" was actually the work Artifactory registry (artifactory.s.o3.ru, inherited from the global ~/.npmrc) being unreachable from the sandbox. Per host instruction (work Artifactory must NEVER be used on personal GitHub projects), a project-local `frontend/.npmrc` now pins `registry=https://registry.npmjs.org/`; with that, `@playwright/test` installs and `npx playwright install chromium` fetches binaries from cdn.playwright.dev fine. IMPL (implementer): `frontend/playwright.config.ts` boots BOTH servers via `webServer` — FastAPI+Socket.IO backend on its sqlite default (port 8100 to dodge a Docker-published :8000, throwaway DB+map dir, `alembic upgrade head` first) and Vite on :5173 with `VITE_E2E=1`. `frontend/e2e/sync.spec.ts` seeds room/map/player/token via the REST API (Node `request` fixture), opens TWO independent browser contexts on their own invite links, waits for BOTH sockets `connected` (so neither misses the broadcast), moves a token on client A, asserts the move line surfaces in client B's combat log (`[data-action-type="move"]` → "moves to (5, 6)") — proves a real server-authoritative broadcast, not faked. Supporting changes: dev-only (`import.meta.env.VITE_E2E`) `window.__e2e` hook in `MapBoard.tsx` exposing the real `handleTokenMove` (Konva draws to <canvas>, so a token can't be dragged via the DOM; branch compiles out of normal builds) + live connection status; e2e-only same-origin Vite proxy in `vite.config.ts` for `/rooms`,`/invites`,`/health`,`/socket.io` (the REST API has NO CORS middleware — Socket.IO does — so a cross-origin browser fetch is blocked; proxy keeps prod/server code untouched); `vitest.config.ts` `include` scoped to `src/**` so Vitest skips the Playwright spec; `package.json` `"e2e": "playwright test"`; `.gitignore` for Playwright artifacts. VERIFIED GREEN by orchestrator: e2e 1 passed (stable across reruns), `npm run build` (tsc -b + vite) clean, oxlint clean, vitest 186 tests passed. ARCHITECT note (NOT a blocker): the REST API lacks CORS; if same-origin is NOT the intended prod topology, adding FastAPI `CORSMiddleware` is the alternative to the e2e proxy — left as future backend work (Backlog candidate).
Next: roadmap is fully ticked — the loop's `roadmap_done` check will stop. No eligible task remains unless the host promotes a Backlog item into a phase.

### Previous focus (Phase 7 error-handling task — DONE)
Phase 7. "Error handling + user-facing messages on desync/disconnect" DONE. Frontend-only, no architect gate (no auth/invite-security/data-deletion or cross-cutting data-model surface; the SERVER already emits `error` events + failure acks from Phase 4 — this task only SURFACES them in the UI; no protocol/version bump, no backend change). PM acceptance criteria (orchestrator-set, UX was fuzzy): (1) connection-health banner on reconnecting, cleared on reconnect; (2) server-rejected intents + join failures shown as dismissible, accessible notices carrying the server's human-readable message; (3) status/notice logic in a pure unit-tested module; (4) server stays authoritative — UI only reflects acks/lifecycle, never invents state. IMPL: new pure React/Konva-free `realtime/status.ts` (mirrors marks.ts/combatLog.ts) — `ConnectionStatus` (connecting/connected/reconnecting), `connectionBanner(status)`→banner|null, capped notice queue (`appendNotice`/`dismissNotice`, `MAX_NOTICES=4`). `realtime/connection.ts` `createBoardSocket` extended w/ `onStatusChange` ('connecting' on create → 'connected' on join-ack-ok → 'reconnecting' on socket `disconnect`/`connect_error`) + `onError` (join-rejection message from the join ack). Action-level rejections flow through each existing `emitAction` ack: MapBoard's new memoised `reportAck` turns `{ok:false,error}` into a dismissible notice (move still also rolls back). MapBoard renders an `aria-live=assertive` reconnecting banner (ONLY for 'reconnecting' — the initial 'connecting' is already covered by the map "Loading map…" `role=status` overlay, avoiding a duplicate role=status collision) + a `role=alert` notices list w/ per-notice dismiss buttons; App.css banner/notice styles.
Phase 7 LAST task "e2e Playwright: two browser clients, one moves → other sees it" is BLOCKED [!] — see NEEDS HUMAN. Investigated this cycle: Playwright is not installed AND there is no network (npm registry ENOTFOUND), so it cannot be installed and browser binaries cannot be fetched; DoD ("tests green") is unsatisfiable in this sandbox. STOPPED per CLAUDE.md rather than guess/half-do. Backend's sqlite default means infra is otherwise ready — the only blocker is obtaining Playwright + browsers (human action required: grant registry/network, OR pre-install browsers, OR sign off a CI-only harness that relaxes local "tests green" for this one task).
Next: AWAITING HUMAN. All other Phase 0–7 roadmap items are [x]; this is the final item and is blocked on infra. The loop should not pick another task until the human resolves NEEDS HUMAN (or promotes a Backlog item into a phase).

### Previous focus (Phase 7 fog-of-war task — DONE)
Phase 7. "Fog of war / hidden tokens controllable by host" DONE. ARCHITECT-gated (touches permissions + the broadcast contract; the migration only ADDS a column — no data deletion, no auth surface → no human gate). Durable `Token.hidden` (default False, server_default "0"). CORE fog enforcement = server-side BoardState FILTERING: `build_board_state(..., include_hidden)` strips hidden tokens AND any character referenced only by a hidden token when building a PLAYER view, so players NEVER receive a hidden token (or its stat block) on the wire (CLAUDE.md rule 3). New HOST-ONLY `setVisibility` intent (additive to the intent union → NO `ACTION_PROTOCOL_VERSION` bump; intent-only like the attack intent — never rebroadcast as an Action). On a visibility change the server pushes a fresh role-filtered `boardState` to per-role Socket.IO sub-rooms (`{room}:host` full / `{room}:player` filtered); a normal action (move/damage/heal/attack) that touches an already-hidden token is emitted to the host sub-room ONLY so the hidden token's movement/HP never leaks. Frontend: types mirror (`TokenResponse.hidden`, `SetVisibilityPayload`, ActionType += setVisibility); host-only `VisibilityControls` (hide/reveal); TokenLayer dims hidden tokens + "hidden" marker; MapBoard wiring. DEFERRED → Backlog: a hidden creature attacking a VISIBLE player token is fully suppressed from that player in v1 (their HP reflects on resync, not live).

### Previous focus (Phase 7 persistence task — DONE)
Phase 7. "Persist room + board snapshots so a session survives a server restart" DONE. Architect finding: the board snapshot was ALREADY fully durable in Postgres and rehydrated by `build_board_state` on every (re)join. The ONE piece of live state that did NOT survive was the in-memory `RoomSequencer` (per-room `seq` reset to 0 → collides with pre-restart seqs). Fix: persist the per-room high-water seq on a NEW durable `Room.last_action_seq` column; the in-memory sequencer is a write-through cache that SEEDS from that column (`RoomSequencer.seed`, idempotent) before vending; `reserve_action_seq` bumps the row in the SAME transaction as the action's durable effect. Layering kept clean via a structural `SequenceCounter` Protocol (no services→realtime cycle).

### Previous focus (Phase 7 permissions task — DONE)
Phase 7 STARTED. "Server-enforced permissions: player acts only on own token; host can do all" DONE — ARCHITECT-AUDIT outcome (exactly as the prior focus predicted: "formalizes/audits existing enforcement rather than new auth"). The literal task = who may ACT on a token = the realtime board-action gate, ALREADY implemented (`services/actions.validate_intent` + `realtime/events.handle_action`) AND exhaustively proven: `test_action_validation.py` is a complete CLAUDE.md-rule-3 matrix (host-any vs player-own vs player-foreign for move/damage/heal/attack, player-without-character rejected, unknown/cross-room token rejected, mark allowed for any participant, full endTurn whose-turn matrix), and `test_realtime.py` proves a player acting on a foreign token is rejected end-to-end with NO broadcast/mutation. Identity is always server-derived from the resolved invite (`JoinedIdentity`), NEVER the client. Verified GREEN this cycle: 59 passed across the two suites. Adding more board-action permission tests = pure duplication (reviewer would reject churn), so NO new tests — the deliverable is the audit + verification + scoping. The genuinely-remaining server-enforcement hole is a DIFFERENT axis — the HTTP room-CONFIG endpoints (place/update token, set initiative, add player, upload map, revoke links) are unguarded (every docstring flags "no auth gate yet") — SPLIT OUT to Backlog because closing it introduces HTTP authentication + needs a host-client-secret-handling decision (the CharacterConfig route holds no token) → touches auth + invite-link security → must NOT be guessed (CLAUDE.md). NOT escalated to NEEDS HUMAN (non-blocking; cleanly backlogged for a future architect-led, human-approved task).
Next: Phase 7 "Persist room + board snapshots so a session survives a server restart" — durable/persistence task; likely ARCHITECT for the snapshot shape + restart rehydration of the only live mutable state (the in-memory `RoomSequencer`) + any Alembic impact.

## NEEDS HUMAN
<!-- Agents put blocking questions here and STOP instead of guessing. -->
(none yet)
<!-- RESOLVED 2026-06-29: the Playwright e2e blocker is gone — root cause was the work Artifactory registry (from the global ~/.npmrc) being unreachable, not a real network outage; project-local frontend/.npmrc now pins the public npm registry. e2e written and GREEN. The only open item is a NON-blocking future-topology decision (CORS vs same-origin proxy), captured in ROADMAP Backlog. Nothing here should stop the loop. -->

## LOG
<!-- Format: [date] [agent] — what changed · what's next -->
- [2026-06-29] orchestrator — Phase 7 LAST task "e2e Playwright: two browser clients, one moves → other sees it" BLOCKED [!] → escalated to NEEDS HUMAN; no specialist delegated, no code changed. Per STATE's prior note I first checked the harness: Playwright is NOT wired (no @playwright/test in frontend/package.json, no playwright.config.*, no e2e/ dir, nothing under node_modules/@playwright or .bin/playwright) and there is NO network — `npm view @playwright/test` → ENOTFOUND (registry artifactory.s.o3.ru unreachable), so `@playwright/test` cannot be installed and `npx playwright install` cannot download browser binaries. DoD requires "tests added AND green"; a Playwright e2e can be neither installed, type-checked, nor run here, so DoD is unsatisfiable — STOPPED per CLAUDE.md instead of shipping an un-runnable spec (would also break tsc/oxlint gates) or faking done. Backend runs on its sqlite default so the only blocker is obtaining Playwright+browsers; human options recorded under NEEDS HUMAN: (a) grant registry/network access, (b) pre-install Playwright+browsers, or (c) sign off a CI-only harness that relaxes local "tests green" for this one task. Bookkeeping only: ROADMAP item → [!], NEEDS HUMAN + CURRENT FOCUS updated. · next: AWAITING HUMAN — do not pick another task until NEEDS HUMAN is resolved (or a Backlog item is promoted).
- [2026-06-29] orchestrator(+pm/implementer/tester/reviewer) — Phase 7 "Error handling + user-facing messages on desync/disconnect" DONE. Frontend-only; no architect gate (no auth/invite-security/data-deletion or cross-cutting data-model surface; the server ALREADY emits `error` events + failure acks from Phase 4 — this task only surfaces them client-side; NO protocol/version bump, NO backend change). PM (orchestrator-set, fuzzy UX): banner on reconnecting/cleared on reconnect; rejected intents + join failures → dismissible accessible notices w/ the server's human-readable message; logic in a pure unit-tested module; server stays authoritative (UI reflects acks/lifecycle, never invents state). IMPLEMENTER: (1) new pure React/Konva-free `realtime/status.ts` (mirrors marks.ts/combatLog.ts) — `ConnectionStatus` (connecting/connected/reconnecting), `connectionBanner(status)`→`ConnectionBanner{message,tone}`|null, capped notice queue `appendNotice`/`dismissNotice` (`MAX_NOTICES=4`, immutable, oldest-dropped). (2) `realtime/connection.ts` `createBoardSocket` + `BoardSocketOptions.onStatusChange` (emits 'connecting' on create → 'connected' after a join-ack-ok → 'reconnecting' on socket `disconnect`/`connect_error`) + `onError` (surfaces the join-rejection message off the join ack; action-level rejections stay on the per-call emitAction ack). (3) `board/MapBoard.tsx`: `connection`+`notices` state, a stable `noticeIdRef` + memoised `pushNotice`/`dismiss`/`reportAck` (turns `{ok:false,error}` into a notice); every action handler (move[+rollback], mark, endTurn, damage, heal, setVisibility, attack) now `.then(reportAck)`; wires `onStatusChange:setConnection`,`onError:pushNotice`; renders an `aria-live=assertive` reconnecting banner (ONLY for 'reconnecting' so it never duplicates the map "Loading map…" `role=status` overlay) + a `role=alert` notices `<ul>` w/ per-item dismiss buttons; App.css `.map-board__connection-banner`(+`[data-tone]`)/`.map-board__notices`/`.map-board__notice`/`.map-board__notice-dismiss`. TESTER: frontend +12 — status.test (7: banner per status, appendNotice append/no-mutate/cap-drops-oldest, dismissNotice match/no-op), connection.test (+2: status across connect→connected→disconnect/connect_error→reconnecting; join rejection → onError without flipping to connected), MapBoard.test (+3: reconnecting banner shows then clears on 'connected'; a rejected damage ack surfaces the server message as a dismissible alert that the dismiss button removes; transport `onError` join-rejection surfaces as an alert) + harness extended (options type +onStatusChange/onError, emitAction reset-per-test so a mockResolvedValueOnce can't leak). REVIEWER (self-gate): server-authoritative — UI only reflects acks + socket lifecycle, never invents board state ✓; reconnect-safe (rule 2) — banner reflects the drop, server re-pushes full BoardState on the existing rejoin ✓; isolated pure module, unit-tested ✓; no protocol/version bump, no auth/invite-security/data-deletion surface ✓. DoD: oxlint clean, prettier --check clean, tsc+vite build clean, vitest 186 passed (+12 vs 174); backend untouched. · next: Phase 7 "e2e Playwright: two browser clients, one moves → other sees it" (LAST Phase-7 item) — check for existing Playwright config/deps first; if the harness isn't wired, may need an architect/pm infra-setup decision.
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 7 "Fog of war / hidden tokens controllable by host" DONE. ARCHITECT (touches permissions + broadcast contract; migration only ADDS a column — no data-deletion/auth surface → no human gate): durable `Token.hidden` (default False, server_default "0", nullable=False); the CORE fog property is SERVER-SIDE BoardState filtering, not client hiding (CLAUDE.md rule 3). `setVisibility` is additive + intent-only (mirrors the attack-intent split) → NO `ACTION_PROTOCOL_VERSION` bump. IMPLEMENTER backend: (1) model `Token` +`hidden: bool`; (2) Alembic `c8d9ea00aa07_add_token_hidden` (batch-add 1 col on tokens; single linear head off `b7c8d9900006`); (3) `schemas/room.TokenResponse` +`hidden: bool=False`; `build_board_state(session, room_id, *, include_hidden=True)` — when False (PLAYER view) drops hidden tokens AND any character referenced only by a hidden token (so a player can neither render nor read a hidden token's stat block off the wire); (4) `schemas/action`: `ActionType.SET_VISIBILITY`, `SetVisibilityPayload{token_id, hidden}` added to `IntentActionPayload` ONLY (not the broadcast union); (5) `services/actions.validate_intent` setVisibility → HOST-ONLY (`_VISIBILITY_HOST_ONLY`) + token must be in room; `apply_action` flips `Token.hidden`; (6) `realtime/events`: join now also enters a role sub-room (`{room}:host`/`{room}:player`) and a player's join board is built with `include_hidden=False`; `handle_action` — a setVisibility applies the flip then pushes a fresh role-FILTERED `boardState` to each sub-room (host full, player filtered) and does NOT rebroadcast an Action, while any normal action touching an already-hidden token (`_action_hidden_from_players`: move/damage/heal target, or attack attacker-or-target) is emitted to the HOST sub-room ONLY (never the base room) so hidden movement/HP never leaks. FRONTEND mirror: `types.ts` (`TokenResponse.hidden?`, `SetVisibilityPayload`, ActionType += setVisibility, added to IntentActionPayload only); host-only `VisibilityControls.tsx` (token select + Hide/Reveal toggle, emits setVisibility); `TokenLayer.tsx` dims hidden tokens (opacity 0.4) + "hidden" marker; `MapBoard.tsx` `handleSetVisibility` + `{isHost && <VisibilityControls>}`; App.css `.map-board__visibility-controls/__visibility-toggle`. TESTER: backend +11 — test_actions (setVisibility narrows via discriminator), test_action_validation (host may set visibility, player rejected, unknown token rejected, apply flips both ways, build_board_state filters hidden tokens+chars for players while host sees all+flag), test_realtime (host setVisibility resyncs role-filtered boards [host keeps flagged token, player drops it, durable hidden persisted, NO action broadcast], player setVisibility rejected+no mutation, move of a hidden token broadcasts to the host sub-room ONLY + still persists) + updated the two join tests for the new role-sub-room enter_room. frontend +5 — VisibilityControls.test (empty→null, hide emits hidden=true, reveal emits hidden=false w/ aria-pressed, targets chosen token) + types.test (setVisibility narrowing). REVIEWER (self-gate): server-authoritative (client emits intent only; hidden flag flipped + filtering done server-side) ✓; HOST-ONLY enforced in validate_intent, players never receive hidden tokens (filtered build + host-only emit, both proven) ✓; reconnect-safe (durable `hidden`; players rejoin to a filtered board, hosts to the full board) ✓; additive schema/no version bump, migration single head + round-trips (mirrors the last_action_seq migration) ✓; no auth/invite-security/data-deletion surface ✓. DEFERRED → Backlog (fog v2): a hidden creature attacking a VISIBLE player token is fully suppressed from that player in v1 (HP on resync, not live); player-filtered board is role-uniform (no per-player own-character special-case). DoD: backend ruff check + ruff format --check clean, mypy --strict clean (39 source files), pytest 325 passed (+11 vs 316), alembic single head verified; frontend oxlint clean, prettier --check clean, tsc+vite build clean, vitest 174 passed (+5 vs 169). · next: Phase 7 "Error handling + user-facing messages on desync/disconnect".
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 7 "Persist room + board snapshots so a session survives a server restart" DONE. ARCHITECT (cross-cutting persistence/data-model; NO auth/invite-security/data-deletion surface → no human gate): the board snapshot is ALREADY fully durable — `Token`/`Character`/`InitiativeEntry`/`Room` rows are the source of truth and `build_board_state` rehydrates the complete BoardState on every (re)join, so the board itself already survives a restart. The ONLY live mutable state that did NOT survive was the in-memory `RoomSequencer` (per-room broadcast `seq` reset to 0 → would collide with pre-restart seqs and break client ordering/de-duplication). Scope = make the action sequence durable. IMPLEMENTER: (1) model `Room` +`last_action_seq: int` (default 0, server_default "0", nullable=False) = the next seq to issue (high-water). (2) Alembic `b7c8d9900006_add_room_last_action_seq` (batch-add 1 col on rooms; single linear head off `a5b6c7800005`; upgrade→downgrade→upgrade round-trips clean on sqlite). (3) `RoomSequencer` +`seed(room_id, next_seq)` — sets the in-memory counter from the durable value when the process hasn't tracked the room yet (post-restart recovery) or when the persisted value is ahead; never rewinds (idempotent). (4) new service `reserve_action_seq(session, *, room_id, sequencer)` — loads the Room, seeds the sequencer from `last_action_seq`, vends `next_seq`, writes `last_action_seq = seq+1`; defensive fall-back to the in-memory counter if the room is somehow absent. To avoid a services→realtime import cycle the service declares a structural `SequenceCounter` Protocol (seed + next_seq) that `RoomSequencer` satisfies — NOT an import of the realtime class. (5) `realtime/events.handle_action` now allocates the seq via `reserve_action_seq` INSIDE the open session, BEFORE commit, so the seq high-water persists in the SAME transaction as the action's durable effect (a crash between applying the action and persisting its seq is impossible). Internal sequencing detail (NOT surfaced in BoardState) → NO `ACTION_PROTOCOL_VERSION` bump, NO frontend mirror. TESTER: backend +3 — `test_realtime.py`: `test_room_sequencer_seed_recovers_after_restart` (seed sets a fresh room's counter, ignores stale/lower, advances on higher), `test_handle_action_persists_seq_high_water_on_room` (two actions → `Room.last_action_seq == 2`), `test_action_seq_survives_server_restart` (process A issues seq 0,1; a BRAND-NEW empty `RoomSequencer` modelling a restart resumes at seq 2, never colliding from 0; row ends at 3). Existing seq tests unchanged-and-green (a fresh sequencer + room at 0 still yields 0,1). REVIEWER (self-gate): server-authoritative seq, allocated server-side atomic with the durable effect, never client-supplied ✓; reconnect-safe rule-2 extended from the board to the action sequence ✓; layering clean via Protocol (no service→transport cycle) ✓; additive migration, round-trips, single head ✓; no auth/security/data-deletion surface ✓. DoD: ruff check + ruff format --check clean, mypy --strict clean (39 source files), pytest 316 passed (+3 vs 313), alembic round-trip verified. Frontend untouched (no contract change). · next: Phase 7 "Fog of war / hidden tokens controllable by host".
- [2026-06-29] orchestrator(+architect) — Phase 7 STARTED. "Server-enforced permissions: player acts only on own token; host can do all" DONE. Routed ARCHITECT first (touches permissions). AUDIT FINDING: the literal task — who may ACT on a token — is ALREADY server-enforced and exhaustively tested from Phase 4/5. `services/actions.validate_intent` is the authoritative gate (identity from the server-resolved invite via `JoinedIdentity`, never the client): token-targeting actions (move/damage/heal) require the token to exist in the actor's room AND, for a player, be bound to their OWN character; an attack authorizes the ATTACKER token the same way + requires the TARGET on-board; endTurn is whose-turn gated (host always / player only on own active combatant); mark carries no token (any participant). `realtime/events.handle_action` runs parse→validate_intent→apply/resolve→broadcast and NEVER broadcasts a rejected intent. Coverage is already a COMPLETE rule-3 matrix: `test_action_validation.py` (host-any vs player-own vs player-foreign across move/damage/heal/attack, player-without-character rejected, unknown-token + cross-room rejected, mark allowed, endTurn before-combat/own-turn/not-your-turn) + `test_realtime.py` (player→foreign token rejected end-to-end, row + HP unchanged, no broadcast). Ran the two suites → 59 passed. Decision (no new code/tests): adding more board-action permission tests would be pure duplication; the deliverable is the audit + green verification + scoping. SPLIT-OUT (Backlog, NOT done here): host-only auth gate on the HTTP room-CONFIG endpoints (place/update token, set initiative, add player, upload map, revoke links) — all currently unguarded ("no auth gate yet"). That is a DIFFERENT axis (who may CONFIGURE a room, not who may ACT on a token) and introduces HTTP authentication (reuse the invite token as `Authorization: Bearer`, assert role=host + room → 401/403) PLUS a host-client-secret-handling decision (the `CharacterConfig` route holds no token) → touches auth + invite-link security → per CLAUDE.md NOT guessed; backlogged for a future architect-led, human-approved task. Did NOT set NEEDS HUMAN (non-blocking — the loop proceeds). DoD: task behavior implemented + tests green (59 passed in the permission suites); ROADMAP ticked; STATE updated; Backlog follow-up recorded. · next: Phase 7 "Persist room + board snapshots so a session survives a server restart".
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 6 "Wire rules engine into the attack/damage actions from Phase 5" DONE → **Phase 6 COMPLETE**. ARCHITECT (cross-cutting rules→protocol/service/data-model; NO auth/invite-security/data-deletion surface → no human gate): scoped conservatively — (1) AC + resistances become DURABLE Character columns (reconnect-safe stat block, rule 2): `armor_class` int (default 10 / server_default "10", bounded 1..50 mirroring `app.rules.attack.MIN/MAX_ARMOR_CLASS`) + `resistances` JSON `{damage_type: defense}` (default {}). (2) Advantage is SERVER-DERIVED from conditions (rule 1), NOT a client field; `damage_type` added as an OPTIONAL attack-intent field (default slashing). (3) ALL schema changes additive → NO `ACTION_PROTOCOL_VERSION` bump (consistent w/ the prior heal-task reasoning). (4) Crit-damage-DICE-doubling DEFERRED — the rules engine exposes no crit-damage helper, so doing it in the service would violate rule 4; this task delivers hit-gating + resistances. IMPLEMENTER: model `Character` +`armor_class`/`resistances`; Alembic `a5b6c7800005_add_character_ac_resistances` (batch-add 2 cols, round-trips, single head). New pure `app.rules.conditions.attack_advantage(attacker_conditions, target_conditions)` (composes attacker own-attack + attacks-against-target sources with the 2024 single-cancellation rule; re-exported via `app.rules.__all__`). `schemas/action.py`: `AttackIntentPayload` +`damage_type: DamageType` (default slashing); `AttackResultPayload` +`advantage`/`armor_class`/`is_hit`/`is_critical_hit`/`is_critical_miss`/`damage_type`/`defense` (damage_rolls empty + damage_total 0 on miss). `schemas/room.py`: AC bounds consts, `AddPlayerRequest` +`armor_class`(ge1 le50 default10) +`resistances` (validator: unknown DamageType/Defense → 422, drops `normal` entries), `CharacterResponse` +`armor_class`/`resistances`. `services/actions.resolve_attack` REWRITTEN to wire the engine: load attacker+target chars → `attack_advantage` → rules `resolve_attack(armor_class=target.AC, attack_bonus, advantage, rng)` → on hit only, `_target_defense` for the damage type → rules `resolve_damage(...)` → apply to durable HP (clamped 0); helpers `_parse_conditions` (ignores stale/unknown names) + `_target_defense` (NORMAL on absent/invalid). `api/rooms.py` passes the 2 new fields into Character creation. FRONTEND mirror (types.ts): `DamageType`/`Defense`/`Advantage` unions; `AddPlayerRequest`/`CharacterResponse` +armor_class/resistances; `AttackIntentPayload` +damage_type?; `AttackResultPayload` +the 7 result fields. `board/combatLog.ts` `formatAttackEntry` now renders "… vs AC N — hit/miss" (+critical hit/miss, +(adv)/(dis), + "(fire resistance)" when defended). TESTER: backend +~13 — test_action_validation rewritten attack tests w/ a local `_ScriptedRandom` (hit applies dmg / miss applies none / nat-20 always hits / target resistance halves / restrained-target grants advantage two-d20s-take-higher), test_actions AttackResult round-trip updated for new fields, test_conditions +5 (`attack_advantage` empty/attacker-adv/target-adv/attacker-disadv/cross-cancel/two-source), test_rooms_api +4 (AC+resistances persist+read-back, defaults, invalid-resistance 422, out-of-range AC 422) + GET asserts new fields. frontend +5 combatLog (hit/miss/crit/resistance/advantage lines) + updated MapBoard/CombatLogPanel log assertions + inserted armor_class/resistances into every CharacterResponse test literal. REVIEWER (self-gate): server-authoritative (rng injected, advantage rules-derived not client-supplied) ✓; rule-4 boundary intact (all tabletop math in app.rules; service only orchestrates; boundary guard green) ✓; reconnect-safe (durable AC/resistances in BoardState, HP applied pre-broadcast) ✓; additive/no version bump, migration round-trips ✓; resistances input validated, no auth/security/data-deletion surface ✓. DoD: backend ruff+format+mypy --strict clean (39 source files), pytest 313 passed (+10); frontend oxlint clean, prettier clean, tsc+vite build clean, vitest 169 passed (+4). · next: Phase 7 "Server-enforced permissions: player acts only on own token; host can do all" — route ARCHITECT first (touches permissions; scope vs the existing `validate_intent` gate).
- [2026-06-29] orchestrator(+implementer/tester/reviewer) — Phase 6 "Conditions (2024 list) with their mechanical effects" DONE. No architect gate (rules-engine boundary already fixed; pure data + helpers, no auth/invite-security/data-deletion or cross-cutting state surface; conditions are NOT yet wired into Character/protocol). IMPLEMENTER: new pure `app/rules/conditions.py` (stdlib + sibling `app.rules.attack` only) — `Condition` StrEnum (the 15 2024 conditions: blinded/charmed/deafened/exhaustion/frightened/grappled/incapacitated/invisible/paralyzed/petrified/poisoned/prone/restrained/stunned/unconscious); frozen `ConditionEffect` dataclass = UNCONDITIONAL mechanical flags (`incapacitated`, `speed_zero`, `attacks_against_have_advantage`/`attacks_against_have_disadvantage`, `own_attacks_have_advantage`/`own_attacks_have_disadvantage`, `ability_checks_have_disadvantage`, `auto_fails_strength_saves`/`auto_fails_dexterity_saves`, `melee_attacks_against_are_critical`) + `effects: tuple[str,...]` authoritative 2024 rules text (carries the CONDITIONAL nuances a single bool can't model — Prone's within-5ft advantage / beyond disadvantage, Charmed's no-attack-the-charmer, Grappled's disadvantage-vs-non-grappler, Petrified's resist-all/poison-immunity, Exhaustion's leveled −2/D20 + −5ft/level); exhaustive `CONDITION_EFFECTS: dict[Condition, ConditionEffect]` (set == set(Condition)); `condition_effect(condition)` lookup; aggregate helpers `incoming_attack_advantage(conditions)` / `outgoing_attack_advantage(conditions)` reusing the sibling `Advantage` enum via private `_combine_advantage` (2024 rule: any advantage source + any disadvantage source → NORMAL; else the present side). Re-exported via `app.rules.__init__` `__all__` (added `CONDITION_EFFECTS`, `Condition`, `ConditionEffect`, `condition_effect`, `incoming_attack_advantage`, `outgoing_attack_advantage`, kept sorted). No DB/schema/protocol change → no migration, no frontend mirror; NO wiring into Character.conditions or the attack/damage flow (that is the explicit later "Wire rules engine into attack/damage actions" task — where conditions/AC/resistances enter the protocol). TESTER: new `tests/test_conditions.py` (+~20 incl. parametrize expansions): pins the 15-condition list, mapping exhaustiveness + every entry self-identifies w/ non-empty effects text, `condition_effect` identity lookup, parametrized flag matrices (attacks-against-advantage: paralyzed/petrified/restrained/stunned/unconscious/blinded; incapacitated set; speed_zero set; auto-fail str+dex saves; melee auto-crit paralyzed/unconscious), invisible disadvantage-against + own-advantage, prone NOT speed_zero but own-disadvantage + neither attacks-against bool (distance nuance in text), restrained dex-save is disadvantage-not-auto-fail, poisoned own+check disadvantage, advantage-aggregation helpers (empty→NORMAL, single advantage/disadvantage source, advantage+disadvantage cancels→NORMAL on both incoming & outgoing), frozen-dataclass immutability. Updated `tests/test_rules_module.py` submodule pin `["abilities","attack","damage","dice"]`→`["abilities","attack","conditions","damage","dice"]` (the `ast` boundary guard still confirms conditions.py imports stdlib + sibling rules only → CLAUDE.md rule-4 boundary intact). REVIEWER (self-gate): pure-data/functions-only, no transport/UI rules math ✓ (outward-import guard green); sibling-only import of `app.rules.attack` (rules-internal, allowed) ✓; additive/no existing importer touched, no protocol/schema/migration change ✓; no auth/security/data-deletion surface ✓. DoD: ruff check + ruff format --check clean, mypy --strict clean, pytest 299 passed (+39 vs 260). · next: Phase 6 "Wire rules engine into the attack/damage actions from Phase 5" (LAST Phase-6 task) — route ARCHITECT first (gates damage on attack-vs-AC hit + applies damage types/resistances + likely surfaces AC/resistances/conditions in the Action schema & Character model → probable protocol-version bump + migration; touches the broadcast contract).
- [2026-06-29] orchestrator(+implementer/tester/reviewer) — Phase 6 "Damage rolls + types; basic resistances" DONE. No architect gate (rules-engine boundary already fixed; pure math, no auth/invite-security/data-deletion surface). IMPLEMENTER: new pure `app/rules/damage.py` (stdlib + sibling `app.rules.dice` only) — `DamageType` StrEnum (the 13 2024 types: acid/bludgeoning/cold/fire/force/lightning/necrotic/piercing/poison/psychic/radiant/slashing/thunder), `Defense` StrEnum (normal/resistance/vulnerability/immunity), `apply_defense(amount, defense)` (PHB multipliers: immunity→0, resistance→`base//2` round-down, vulnerability→`base*2`, normal→unchanged; `base=max(amount,0)` so never negative), `resolve_damage(*, expression, damage_type, defense=NORMAL, rng)` → frozen `DamageResult{rolls,modifier,damage_type,defense,raw_total,total}`: composes `roll_dice(expression, rng=rng)` (server-authoritative injected `random.Random`), clamps `raw_total=max(roll.total,0)` then applies defense → final `total`; raises `DiceExpressionError` (reused from dice) on a malformed expression. Re-exported via `app.rules.__init__` `__all__` (`from app.rules import resolve_damage` — `DamageResult`, `DamageType`, `Defense`, `apply_defense`, `resolve_damage` added, kept sorted). No DB/schema/protocol change → no migration, no frontend mirror; NO wiring into the Phase-5 always-applies damage flow (that is the explicit later "Wire rules engine into attack/damage actions" task — where resistances live in the protocol). TESTER: new `tests/test_damage.py` (+20, seeded/scripted `_ScriptedRandom` for deterministic dice): DamageType covers exactly the 13 types, Defense members, parametrized `apply_defense` matrix (normal/resist 10→5 + odd 7→3 + 1→0 round-down/vuln 10→20 + 0→0/immunity→0 + negative-amount clamps ≥0 across normal/vuln/resist), resolve normal rolls+classifies, resistance halves rounding down (12→6 and odd 7→3), vulnerability doubles (12→24), immunity zeroes (raw 15→0), flat expression `"4"` has no rolls, malformed expr → DiceExpressionError, same-seed determinism. Updated `tests/test_rules_module.py` submodule pin `["abilities","attack","dice"]`→`["abilities","attack","damage","dice"]` (the `ast` boundary guard still confirms damage.py imports stdlib + sibling rules only → CLAUDE.md rule-4 boundary intact). REVIEWER (self-gate): pure-functions-only, no transport/UI rules math ✓ (outward-import guard green); server-authoritative roll (rng injected, client never supplies the dice) ✓; additive/no existing importer touched, no protocol/schema/migration change ✓; no auth/security/data-deletion surface ✓. DoD: ruff check + ruff format --check clean, mypy --strict clean (62 source files), pytest 260 passed (+20). · next: Phase 6 "Conditions (2024 list) with their mechanical effects" — another pure `app/rules` module (condition enum + mechanical-effect data); implementer→tester→reviewer, likely no architect gate unless a cross-cutting state/data-model shape is needed.
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 6 "Attack roll vs AC; advantage/disadvantage" DONE. ARCHITECT (cross-cutting rules call, but boundary already fixed; NO auth/invite-security/data-deletion surface → no human gate): scoped this as a PURE rules-engine task mirroring the "Ability scores" task — added the attack-resolution MATH only; deliberately scoped OUT any Action/schema change, Character-AC field, and wiring into the Phase-5 always-hits attack flow → that integration (where AC lives in the protocol, gating applied damage on a hit) is the explicit later "Wire rules engine into the attack/damage actions" task. So NO migration, NO frontend mirror, NO protocol-version bump. IMPLEMENTER: new pure `app/rules/attack.py` (stdlib + sibling `app.rules.dice` only) — `Advantage` StrEnum (normal/advantage/disadvantage), `resolve_attack(*, armor_class, attack_bonus=0, advantage=NORMAL, rng)` → frozen `AttackRoll{rolls,advantage,d20,attack_bonus,total,armor_class,is_critical_hit,is_critical_miss,is_hit}`: rolls 1 d20 (normal) or 2 (adv/disadv via `d20_count`) + selects per `select_d20` (max/min/first), adds flat bonus, decides hit via `attack_hits` (2024 rules: nat-20=`NATURAL_CRIT` always hits, nat-1=`NATURAL_MISS` always misses, else total≥AC); server-authoritative RNG injected as `random.Random`. Bounded constants MIN/MAX_ARMOR_CLASS (1/50), MIN/MAX_ATTACK_BONUS (-20/20, mirrors the Action schema's attack_bonus); `ArmorClassError`/`AttackBonusError` raised out-of-range. Re-exported via `app.rules.__init__` `__all__` (`from app.rules import resolve_attack`). TESTER: new `tests/test_attack.py` (+23, seeded `_ScriptedRandom` for deterministic d20 scripting): d20_count, select_d20 (adv max/disadv min/normal first/empty→ValueError), attack_hits matrix (meets-AC/under/over/nat20-vs-AC50/nat1-vs-huge-total), resolve normal hit+bonus / miss-below-AC / nat-20 crit auto-hit even vs AC50 / nat-1 auto-miss even +20, advantage takes higher / disadvantage takes lower, out-of-range AC (0/51/-1)→ArmorClassError, out-of-range bonus (±21)→AttackBonusError, same-seed determinism. Updated `tests/test_rules_module.py` submodule pin `["abilities","dice"]`→`["abilities","attack","dice"]` (the `ast` boundary guard still confirms attack.py imports stdlib + sibling rules only → CLAUDE.md rule-4 boundary intact). REVIEWER (self-gate): pure-functions-only, no transport/UI rules math ✓ (outward-import guard green); server-authoritative roll (rng injected, client never supplies the die) ✓; additive/no existing importer touched, no protocol/schema/migration change ✓; no auth/security/data-deletion surface ✓. DoD: ruff check + ruff format --check clean, mypy --strict clean (54 source files), pytest 240 passed (+23). · next: Phase 6 "Damage rolls + types; basic resistances" — another pure `app/rules` module composing `roll_dice` with damage types + resistance/vulnerability/immunity multipliers; implementer→tester→reviewer, no architect gate.
- [2026-06-29] orchestrator(+implementer/tester/reviewer) — Phase 6 "Ability scores, modifiers, proficiency bonus" DONE. No architect gate (rules-engine boundary already fixed; pure math, no auth/invite-security/data-deletion surface). IMPLEMENTER: new pure `app/rules/abilities.py` (stdlib-only) — `ability_modifier(score)=(score-10)//2` (Python floor div matches `floor((score-10)/2)` incl. odd low scores; mirrors frontend `screens/abilityModifier.ts`), `proficiency_bonus(level)=2+(level-1)//4` (+2 @1-4 … +6 @17-20), `format_modifier` (signed string), `Ability` StrEnum (str/dex/con/int/wis/cha codes), frozen `AbilityModifier` dataclass (`.signed` prop), bounded constants MIN/MAX_ABILITY_SCORE (1/30, matches AbilityScores Pydantic), MIN/MAX_LEVEL (1/20), MIN/MAX_PROFICIENCY_BONUS (2/6); `AbilityScoreError`/`LevelError` raised out-of-range. Re-exported via `app.rules.__init__` `__all__` (the supported public surface — consumers `from app.rules import ability_modifier`). No DB/schema/protocol change → no migration, no new frontend contract (display helper already exists). TESTER: new `tests/test_abilities.py` (parametrized: modifier formula incl. score 1→-5/9→-1/30→10, out-of-range 0/31 → AbilityScoreError, format_modifier signs, proficiency by level 1..20 boundaries, out-of-range level → LevelError, monotonic-non-decreasing bonus, Ability enum codes/lookup, AbilityModifier.signed +3/-1). Updated `tests/test_rules_module.py` submodule pin `["dice"]`→`["abilities","dice"]` (guard still confirms abilities imports stdlib only — boundary intact). REVIEWER (self-gate): pure-functions-only, no transport/UI rules math ✓; outward-import guard green ✓; additive/no existing importer touched ✓; no auth/security/data-deletion surface ✓. DoD: ruff check + ruff format --check clean, mypy --strict clean (36 source files), pytest 217 passed (+40). · next: Phase 6 "Attack roll vs AC; advantage/disadvantage" — composes roll_d20 + ability modifier + proficiency vs an AC; likely ARCHITECT for AC location (Character field?) + attack-result shape vs the Phase-5 basic-always-hits flow.
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 6 STARTED. "Isolated rules module (pure functions, fully unit-tested)" DONE. ARCHITECT (cross-cutting rules-engine boundary per CLAUDE.md rule 4; NO auth/invite-security/data-deletion surface → no human gate): this foundational task FORMALIZES the `app.rules` boundary rather than adding more rules content (ability/attack/damage/conditions are the SUBSEQUENT Phase-6 tasks) — decisions: (1) the package's supported public API is `app.rules.__all__`; consumers should `from app.rules import roll_dice` (deep `app.rules.dice` imports still work — fully additive, the existing schemas/action.py + services/actions.py importers untouched); (2) "isolated" is now MECHANICALLY enforced, not just documented. BACKEND (additive, no production behavior change): `app/rules/__init__.py` re-exports the dice primitives (`D20_SIDES`, `MAX_*`, `DiceExpressionError`, `DiceRoll`, `ParsedDice`, `parse_dice`, `roll_d20`, `roll_dice`, `roll_die`) under `__all__` + documents the boundary contract (pure: stdlib + sibling rules modules only; server-authoritative RNG injected as `random.Random`). NEW `tests/test_rules_module.py` (+4): an `ast`-based guard parses every `*.py` under `app/rules` and FAILS if any imports a forbidden outer layer (`app.api/realtime/db/models/services/schemas/storage/security/main/config`) or framework (fastapi/starlette/socketio/sqlalchemy/alembic/pydantic/httpx) → a future rules task that reaches outward breaks CI immediately; plus pins package surface (only the `dice` submodule, no stray sub-packages), `__all__` importability, and re-export identity-match with `app.rules.dice`. No DB/schema/protocol change → no migration, no frontend mirror. REVIEWER (self-gate): pure-functions-only (no rules math added to transport/UI), boundary direction correct (outer layers import rules, never reverse — confirmed by guard) ✓, additive/no breakage of existing deep importers ✓, no auth/security/data-deletion surface ✓. DoD: ruff check + ruff format --check clean, mypy --strict clean (50 source files), pytest 177 passed (+4). · next: Phase 6 "Ability scores, modifiers, proficiency bonus" — new pure `app/rules` module (modifier `floor((score-10)/2)` mirroring the frontend `abilityModifier.ts` + proficiency-bonus-by-level); implementer→tester→reviewer, no architect gate (boundary now fixed).
- [2026-06-29] orchestrator(+implementer/reviewer) — Phase 5 "Shared combat log panel" DONE → **Phase 5 COMPLETE**. Frontend-only; no auth/invite-security/data-deletion or cross-cutting data-model surface (log stays transient/client-built like pings) → no architect gate. Generalized the attack-only combat log into a full shared panel covering EVERY broadcast action type. `board/combatLog.ts` (still React/Konva-free, unit-tested): `CombatLogEntry.payload` widened `AttackResultPayload`→`BroadcastActionPayload`; replaced `attackLogEntry` (null-for-non-attack) with `logEntry(action)` (always builds an entry for ANY Action); made `formatAttackEntry` private + folded into new exported `formatLogEntry(payload,nameOf)` that switches on `payload.type` — move `"X moves to (x, y)"`, mark `"Ping at (x, y)"` (+ `"Ping \"label\" at …"` when labelled), damage `"X takes N damage"`, heal `"X heals N HP"`, attack (unchanged format), endTurn `"Turn ended"`; `MAX_LOG_ENTRIES 50`/`appendLogEntry` unchanged. `board/CombatLogPanel.tsx`: uses `formatLogEntry`, adds `aria-live=polite`, per-line `data-action-type`, and a scroll ref that auto-sticks to the newest line as entries arrive. `board/MapBoard.tsx`: onAction now logs EVERY broadcast action ONCE at the top via `appendLogEntry(logEntry(action))` (removed the attack-only append + `attackLogEntry` import); the type-specific board/HP/initiative state updates below are unchanged. No backend/protocol change. REVIEWER (self-gate): server-authoritative (lines built only from server-stamped broadcast Actions; client invents nothing) ✓, transient/ephemeral consistent with prior ping decision; reconnect rebuilds from boardState, no durable log surface ✓, names resolved at render time from current tokens ✓, no auth/security surface ✓. DoD: oxlint clean, prettier clean, tsc+vite build clean, vitest 165 passed (+7: combatLog.test 6→12 [logEntry both-branches + formatLogEntry per type incl. labelled/plain ping + endTurn], CombatLogPanel.test 2→3 [mixed move/damage/endTurn lines in order + data-action-type]). · next: Phase 6 "Isolated rules module (pure functions, fully unit-tested)" — grows the Phase-5 `app/rules/dice.py` seed; likely route ARCHITECT for rules-engine module shape/boundaries (CLAUDE.md rule 4).
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 5 "Basic attack flow: choose target → roll → apply result, all see the log" DONE. ARCHITECT (cross-cutting roll-math location + Action shape; NO auth/invite-security/data-deletion surface → no human gate): (1) roll math ISOLATED in NEW pure `app/rules/dice.py` (seed of the Phase-6 rules engine, rule 4) + injectable `random.Random` → server-authoritative (rule 1) yet deterministic under test; (2) attack intent≠broadcast so the protocol union SPLIT into `IntentActionPayload`/`BroadcastActionPayload` (both share move/mark/damage/heal/endTurn — additive, parse-time-safe → NO version bump); (3) BASIC = always lands + applies rolled damage (no AC gating; hit/miss-vs-AC is Phase 6), but `attack_total` carried for later. BACKEND: new `app/rules/__init__.py`+`dice.py` — `D20_SIDES`, bounds `MAX_DICE_COUNT 50`/`MAX_DICE_SIDES 1000`/`MAX_MODIFIER 1000`/`MAX_FLAT_AMOUNT 1000`, `DiceExpressionError`, `ParsedDice`/`DiceRoll`, `parse_dice` (NdS(+/-M)/flat, whitespace-tolerant), `roll_die`/`roll_d20`/`roll_dice`. `schemas/action.py` +`ActionType.ATTACK`, +`ATTACK_BONUS_MIN/MAX(-20/20)`/`DAMAGE_EXPR_MAX_LENGTH 32`/`DEFAULT_ATTACK_DAMAGE "1d6"`, +`AttackIntentPayload{attacker_token_id,target_token_id,attack_bonus,damage}` (field_validator parses `damage` via `parse_dice` → 422 on bad expr) + `AttackResultPayload{attack_roll 1..20,attack_bonus,attack_total,damage,damage_rolls[],damage_total ge0}`; split `IntentActionPayload`(+AttackIntent) vs `BroadcastActionPayload`(+AttackResult), `ActionPayload`=intent alias, `ActionIntent.payload:IntentActionPayload`, `Action.payload:BroadcastActionPayload`. `services/actions.py` — `validate_intent` +attack branch (authorize ATTACKER via `_authorize_token_target` host-any/player-own + new `_require_token_in_room` for TARGET), new `resolve_attack(session,*,room_id,payload,rng)` (rolls d20+bonus + damage dice, applies damage to target Character.current_hp clamped 0 BEFORE broadcast, returns AttackResultPayload). `realtime/events.py` — `handle_action` +`rng` param (default module `_DEFAULT_RNG`), routes AttackIntentPayload→`resolve_attack` else `apply_action`, broadcasts `broadcast_payload`; `register_handlers` builds one process rng. NO DB/schema change → no migration. FRONTEND mirror (types.ts): +'attack' ActionType, `AttackIntentPayload`/`AttackResultPayload`, split `IntentActionPayload`/`BroadcastActionPayload`, `ActionPayload`=intent alias, `ActionIntent`/`Action` payloads retyped. New pure `board/combatLog.ts` (React/Konva-free, unit-tested) — `CombatLogEntry`, `MAX_LOG_ENTRIES 50`, `attackLogEntry` (null for non-attack), `appendLogEntry` (cap newest), `formatAttackEntry(payload,nameOf)`. New `board/CombatLogPanel.tsx` (role=log overlay; named distinct from combatLog.ts to avoid case-only filename clash, mirrors marks.ts/MarkLayer.tsx convention) + `board/AttackControls.tsx` (attacker select from controllable tokens, target select, bonus + damage inputs, Attack button; emits intent only). WIRED `MapBoard.tsx`: onAction 'attack' branch → live target HP via `applyHpAction` synthetic damage + `appendLogEntry`; `handleAttack` emits attack intent; `controllableTokenIds = tokens.filter(canDrag)`; renders `<AttackControls>` (when controllable) + `<CombatLogPanel>`. CSS `.map-board__attack-controls`/`.map-board__attack-button`/`.map-board__combat-log`. TESTER: backend +27 — test_dice.py (parse simple/defaults/neg+ws/flat, parametrized bad-expr, roll ranges, det seed, flat no-rolls), test_actions.py (+4: attack narrows w/ defaults, bad damage expr 422, attack_bonus bounds ±21, AttackResult broadcast round-trip), test_action_validation.py (+5: host attacks any, player attacks from own, player CANNOT attack from foreign token, target must be on board, resolve_attack rolls+applies clamped damage), test_realtime.py (+1: host attack resolves server-side seeded rng → broadcasts AttackResult + target HP drops by rolled damage). frontend +13 — combatLog.test (6), CombatLogPanel.test (2: empty→null, renders line resolving names), AttackControls.test (3: null w/o controllable / null <2 tokens / emits w/ chosen attacker+target+bonus+damage), MapBoard.test (+2: attack broadcast drops HP 25→18 + logs line, host Attack button emits attack intent). REVIEWER: server-authoritative roll (client emits intent only; dice rolled server-side, HP reconciled to broadcast) ✓, perms server-enforced (attacker controllable / target on board; AttackControls cosmetic) ✓, additive protocol/no version bump ✓, roll math isolated+unit-tested both sides ✓, reconnect-safe (durable HP applied pre-broadcast; log transient like pings) ✓. DoD: backend ruff+format+mypy --strict clean (35 source files), pytest 173 passed (+27); frontend oxlint clean, prettier clean, tsc+vite build clean, vitest 158 passed (+13). · next: Phase 5 "Shared combat log panel" — generalize the transient attack-only combat log into a full shared panel (more entry types, scrolling); frontend-led, no architect gate unless durable/replayable on-(re)join state is wanted.
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 5 "Apply damage/healing to a token; HP updates broadcast live" DONE. ARCHITECT (cross-cutting protocol shape; NO auth/invite-security/data-deletion surface → no human gate): healing = a NEW additive `heal` ActionType (NOT a signed DamagePayload) — the discriminated union is additive + parse-time-safe so NO `ACTION_PROTOCOL_VERSION` bump; a signed amount would have to widen the existing `amount ge=1` bound (incompatible change) + conflate two distinct events (bad for the coming combat log). Heal clamps at max_hp (mirror of damage clamp at 0); same `_authorize_token_target` perm (host any token in room / player only own). BACKEND: `schemas/action.py` +ActionType.HEAL, +HEAL_AMOUNT_MIN/MAX(1/1000), +HealPayload, added to the ActionPayload union; DamagePayload docstring updated (damage/heal deliberately separate). `services/actions.py` — `validate_intent` token-target isinstance now incl. HealPayload; `apply_action` +heal branch (current_hp = min(max_hp, current_hp+amount)). NO DB/schema change → no migration. FRONTEND mirror: types.ts +'heal' in ActionType, +HealPayload, added to ActionPayload union. New pure `board/hp.ts` (React/Konva-free, unit-tested) — `clampHp(value,maxHp)` (floor 0/ceil max), `applyHpAction(characters, characterId, payload)` (damage subtract/heal add, clamped; SAME-REF return when unchanged so React bails). New `board/HpControls.tsx` (host-only: target `<select>` + amount input + Damage/Heal buttons; emits intent only — server broadcast drives the HP update, no optimistic change). WIRED `MapBoard.tsx`: `boardRef` (always-fresh, so the once-registered onAction can resolve token→character without a stale closure); onAction damage|heal branch → resolve target token's character via boardRef → `setCharacters(applyHpAction(...))` (live HP without waiting for a full boardState push); handleDamage/handleHeal emit damage/heal intents; `{isHost && <HpControls …>}` overlay. CSS `.map-board__hp-controls`/`.map-board__hp-damage`/`.map-board__hp-heal`. TESTER: backend +6 — test_actions (heal narrows via discriminator, parametrized heal-amount bounds 0/-1/1001), test_action_validation (host may heal any / player may heal own / player may NOT heal another's token), test_realtime (host heal persists clamped at max_hp 24 + broadcasts). frontend +12 — hp.test (6: clampHp floor/ceil/non-positive-max, damage clamp 0, heal clamp max, only-targeted, same-ref no-op for unknown/dead/non-hp), HpControls.test (3: empty→null, emits damage w/ amount, emits heal for chosen target), types.test (+1 heal narrowing), MapBoard.test (+2: damage broadcast updates rendered HP 20/30→15/30, host Damage button emits damage intent). REVIEWER: server authoritative (client emits intent only; HP reconciled strictly to broadcast, no optimism), perms server-enforced (HpControls cosmetic), heal additive/no version bump, HP math isolated+unit-tested both sides, reconnect-safe (durable Character.current_hp rebuilt in BoardState). DoD: backend ruff+format+mypy --strict clean (46 files), pytest 146 passed (+6); frontend oxlint clean, prettier clean, tsc+vite build clean, vitest 145 passed (+12). · next: Phase 5 "Basic attack flow: choose target → roll → apply result, all see the log" — first dice/roll task; route via ARCHITECT (roll math location anticipating the Phase-6 rules engine + the attack/roll-result Action shape) and/or pm for acceptance criteria; composes the heal/damage actions to apply results.
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 5 "Initiative tracker + turn order; 'end turn' advances it" DONE. ARCHITECT (cross-cutting data model; the `set_initiative` DELETE is config replacement scoped to the room's own initiative rows = analogous to map re-upload, NOT user-content/room deletion, so within guardrails): turn order is DURABLE (reconnect-safe rule 2) — a reloading client must see the full order + whose turn it is, so it cannot be memory-only. BACKEND: new model `app/models/initiative.py::InitiativeEntry` (id, room_id FK CASCADE idx, character_id FK CASCADE nullable idx [NULL=NPC], name, initiative, order_index) + Room.{initiative_active_index:int|None, initiative_round:int=1} + Room.initiative_entries relationship; registered in models/__init__. Alembic `f4a5b6700004_add_initiative` (batch add 2 room cols + create initiative_entries + 2 indexes; single head; upgrade→downgrade round-trips on sqlite). Schemas (`schemas/room.py`): InitiativeEntryInput (character_id?/name/initiative ge-50 le100), SetInitiativeRequest (entries ≤100, empty clears), InitiativeEntryResponse, InitiativeState{active_index,round,entries}; BoardState gained `initiative: InitiativeState`. Service `app/services/initiative.py`: `set_initiative` (DELETE old → sort by initiative desc, stable ties → seat 0..n-1 → active=0 if any else None, round=1), `advance_turn` (next seat; wrap→0 + round++; no-op without order), `build_initiative_state`, `active_character_id` (combatant whose turn it is). Wired: `board.py::build_board_state` now embeds initiative; `actions.py::apply_action` endTurn→advance_turn; `actions.py::validate_intent` endTurn→`_authorize_end_turn` (host always; player only on own active combatant; else _COMBAT_NOT_STARTED/_NOT_YOUR_TURN). HTTP host config (`api/rooms.py`): `PUT /rooms/{id}/initiative` (404 room, 422 foreign character) + `GET .../initiative` (reconnect-safe read). FRONTEND mirror: types.ts InitiativeEntryResponse/InitiativeState + BoardState.initiative; new pure `board/initiative.ts` (advanceInitiative mirrors server [same-ref no-op when no order so React bails], activeEntry, isActiveCharacter, EMPTY_INITIATIVE); new `board/InitiativeTracker.tsx` (HTML overlay: round, ordered combatants w/ active highlighted via aria-current, "End turn" button enabled only when host OR own active turn). WIRED `MapBoard.tsx`: hold InitiativeState, onBoardState→setInitiative, onAction endTurn branch→advanceInitiative (boardState is reconnect-safe source of truth), handleEndTurn→emitAction({type:'endTurn'}), `<InitiativeTracker>` overlay. CSS `.initiative-tracker*`. TESTER: backend +19 — `test_initiative.py` (8: set sorts/resets, empty clears, replaces, advance next/wrap+round/noop, active_character_id tracks, build empty), `test_rooms_api.py` (+6 PUT sorts+active / GET reads back / empty clears / 404 room x2 / 422 foreign char), `test_realtime.py` (+2 player endTurn advances+broadcasts / rejected-not-their-turn no mutation), `test_action_validation.py` (replaced obsolete "any participant may endTurn" with host-always / player-before-combat-rejected / player-own-turn / player-not-their-turn). frontend +14 — initiative.test (8), InitiativeTracker.test (4: empty→null, round+active aria-current, host ends turn, player enabled only on own turn), MapBoard.test (+2: tracker from boardState + emits endTurn, advances on endTurn broadcast). REVIEWER: server authoritative (client emits intent only), perms enforced server-side (button is cosmetic), reconnect-safe (durable rows + BoardState rebuild; client advance is optimistic-then-reconciled), turn logic isolated+unit-tested both sides. DoD: backend ruff+format+mypy --strict clean (46 files), pytest 138 passed (+19), alembic round-trip OK; frontend oxlint clean, prettier clean, tsc+vite build clean, vitest 133 passed (+14). · next: Phase 5 "Apply damage/healing to a token; HP updates broadcast live" — DamagePayload already persists+broadcasts HP; likely add healing (new ActionType vs signed amount → route ARCHITECT for protocol-version impact) + client HP-change affordance + reconciling damage/heal broadcasts into live token HP.
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 5 "Marks / pings on the board (temporary, visible to everyone)" DONE. ARCHITECT (no auth/invite-security/data-deletion surface → no human gate): marks are EPHEMERAL — backend ALREADY validates (`mark` = non-token payload any participant may issue) + broadcasts via the existing `handle_action` pipeline, `apply_action` correctly a no-op (NO Mark row, NO BoardState.marks); so NO backend production change. Rule 2 (reconnect-safe) governs DURABLE state — a ping is transient by design (reconnecting client won't see prior pings). Server broadcasts to the whole room incl. sender, so NO optimistic overlay needed. FRONTEND: new pure `board/marks.ts` (React/Konva-free, unit-tested) — `BoardMark{id,x,y,color?,label?,expiresAt}`, `MARK_TTL_MS=4000`, `MAX_MARKS=50`, `markFromAction` (null for non-mark; id=action.id), `addMark` (dedupe+cap oldest), `pruneExpired` (same-ref when unchanged → React bail); new `MarkLayer.tsx` (Konva Group/Circle ring+dot at cell centre + optional label, listening=false). WIRED `MapBoard.tsx`: `onAction` mark branch → addMark(pruneExpired); 500ms setInterval prunes expired (fade-out); "Ping" toggle button (aria-pressed) disables stage pan + turns a board click into screenToWorld→worldToCell→`emitAction({type:'mark',x,y})`; `<MarkLayer>` above TokenLayer; CSS `.map-board__ping-toggle(.is-active)`. TESTER: backend +1 `test_realtime.py::test_handle_action_player_mark_broadcasts_without_mutation` (player mark broadcasts to whole room, payload echoed, token cell + HP UNCHANGED); frontend +15 (marks.test 9, MarkLayer.test 4, MapBoard.test +2: ping toggle aria-pressed, onAction mark renders a Circle via captured socket options). REVIEWER: server authoritative (client only emits intent), ephemeral/no durable surface (proven by no-mutation test), any participant may ping (intended), reconnect untouched. DoD: backend ruff+format+mypy --strict clean (43 files), pytest 119 passed (+1); frontend oxlint clean, prettier clean, tsc+vite build clean, vitest 119 passed (+15). · next: Phase 5 "Initiative tracker + turn order; 'end turn' advances it" — first durable turn-order surface; route via ARCHITECT for where turn order lives + how endTurn advances + whose-turn enforcement.
- [2026-06-29] orchestrator(+implementer/tester/reviewer) — Phase 5 STARTED. "Move token with grid snapping + distance measurement (feet)" DONE. Frontend-only, no auth/invite-security/data-deletion surface → no architect gate. Grid SNAPPING already shipped Phase 4 (`worldToCell` on drag-end); this task adds the D&D 2024 distance-in-feet readout. NEW pure `grid.ts::cellDistanceFeet(from,to,feetPerCell=DEFAULT_FEET_PER_CELL=5)` = Chebyshev cell distance × 5 (2024 PHB: every square incl. diagonals = 5 ft; symmetric; defensive 0 for non-finite/non-positive scale) — kept React/Konva-free + unit-tested (rule 4). WIRED into `TokenLayer.tsx`: controllable token drag now wires `onDragMove` → candidate cell via `worldToCell` → live "<n> ft" Text measured from origin cell; cleared on `onDragEnd` (still snaps + reports cell via `onMove`, unchanged). REVIEWER: measurement is UI-only/informational, server stays authoritative (rule 1), only offered for `canDrag` tokens (cosmetic; server still validates), no persistence/reconnect surface. DoD: oxlint clean, prettier clean, tsc+vite build clean, vitest 104 passed (+9: grid cellDistanceFeet 6, TokenLayer drag/measure 3). · next: Phase 5 "Marks / pings on the board (temporary, visible to everyone)" — MarkPayload exists in the protocol but `apply_action` is a no-op for it; route via ARCHITECT for ephemeral-vs-durable mark storage.
- [2026-06-29] orchestrator(+tester) — Phase 4 "Reconnect test: reload a player link mid-encounter, state restores" DONE → **Phase 4 COMPLETE**. Test-only task (no production code, no auth/invite-security/data-deletion surface → no architect gate). Added backend integration-style `test_realtime.py::test_reload_player_link_mid_encounter_restores_state` exercising the already-wired boardState-on-rejoin path over the seeded in-memory DB via the real handler fns: (1) player `handle_join` → asserts initial snapshot token at (3,4) + character HP 20; (2) host (bound `JoinedIdentity`) runs TWO `handle_action`s through a shared `RoomSequencer` + `seeded.factory` — `move`→(7,8) and `damage`→-5 — each applies to durable rows, commits, broadcasts; (3) player RELOADS its link on a FRESH connection (`p-sid2`) → `handle_join` pushes the rebuilt BoardState reflecting (7,8) + HP 15 (not the stale start) to `to=p-sid2`, and re-binds the resolved `JoinedIdentity` (participant_id matches) so the reconnected client can keep acting. Proves CLAUDE.md rule 2: durable Token/Character rows are source-of-truth, snapshot is rebuilt idempotently on every (re)join, reconnecting client never needs to have seen the intervening Actions. DoD: backend ruff check + ruff format --check clean, mypy --strict clean (43 files), pytest 118 passed (+1). · next: Phase 5 "Move token with grid snapping + distance measurement (feet)" (first combat-actions task).
- [2026-06-29] orchestrator(+implementer/tester/reviewer) — Phase 4 "Optimistic move on client + reconcile to server broadcast" DONE. Frontend-only (consumes the already-shipped `emitAction`/`onAction`/`onBoardState` transport); NO backend, NO new auth/invite-security/data-deletion surface → no architect gate. NEW pure module `frontend/src/board/reconcile.ts` (kept free of React/Konva per the board pattern so rule-5 logic is unit-testable): `ReconcilableBoard` = server-authoritative token Map + thin optimistic-overlay Map; `fromBoardState`/`fromTokens` (build truth, clear overlay), `beginOptimisticMove` (overlay a move; ignores unknown token), `applyAction` (move→update authoritative + DROP overlay so a match is seamless & a mismatch snaps to server = rollback; non-move/unknown = identity), `rollbackMove` (drop overlay on rejected ack), `displayTokens` (truth+overlay). NEW pure `tokens.ts::worldToCell` (inverse of `tokenRect`; rounds a dropped world point→grid cell, clamped ≥0 = server GRID_COORD_MIN). WIRED: `MapBoard` now takes `{token,isHost?,controllableCharacterId?}`, opens `createBoardSocket(token,{onBoardState→fromBoardState reset (reconnect-safe rule 2), onAction→applyAction})` in an effect (disconnect on teardown, socketRef), REST hydrate seeds `fromTokens` for instant paint then the boardState push takes over; `canDrag` (host=any / player=own-character token only — UI affordance; server `validate_intent` still authoritative), `handleTokenMove`→`beginOptimisticMove`+`emitAction(move)`→on `ok:false` `rollbackMove` (rule 5). `TokenLayer` gains optional `canDrag`/`onMove`: controllable tokens become a draggable Konva Group whose `onDragEnd` snaps via `worldToCell`, resets the Group offset, and reports the cell (non-controllable stay `listening=false`). `JoinScreen` passes token + role/character identity into MapBoard. REVIEWER: identity from resolved invite (not client), server still enforces perms (canDrag is cosmetic), optimistic reconciled→rollback on mismatch/reject ✓, reconnect re-pushes full board ✓. DoD: oxlint clean, prettier clean, `tsc -b && vite build` clean, vitest 95 passed (+13: reconcile 10, worldToCell 3); MapBoard.test mocks `../realtime/connection` (fake socket) + adds `token` prop. · next: Phase 4 "Reconnect test: reload a player link mid-encounter, state restores" (last Phase 4 task).
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 4 "Broadcast resulting Action to all participants in the room" DONE. ARCHITECT (broadcast wiring reusing the already-approved invite identity → NO new auth/invite-security/data-deletion surface, no human gate): (1) per-sid identity uses Socket.IO's built-in `save_session`/`get_session` — on a successful join the resolved `JoinedIdentity{room_id, role, participant_id, character_id}` is stashed; no new global dict, auto-dropped on disconnect; (2) `seq` from a tiny in-memory `RoomSequencer` (dict room→int, zero-based monotonic) created ONCE per server in `register_handlers` — the ONLY live mutable state; durable Token/Character rows remain source-of-truth (consistent with "read rows, no in-memory BoardState store"); (3) validated `move`/`damage` are APPLIED to the durable rows BEFORE broadcast so `build_board_state` stays authoritative → mid-encounter reconnect rebuilds the post-action board (rule 2). IMPLEMENTER: backend — `app/services/actions.py` +`apply_action(session, *, room_id, payload)` (move→Token.x/y; damage→Character.current_hp clamped at 0; mark/endTurn no-op; defensive room-match guards); new `app/realtime/sequence.py::RoomSequencer`; `app/realtime/events.py` — `JoinedIdentity` dataclass, `handle_join` now `save_session`s the identity, new `handle_action(sio, sid, data, *, sequencer, session_factory)` (load identity→`_NOT_JOINED_ERROR` if absent → parse `ActionIntent`→`_MALFORMED_ACTION_ERROR` on `ValidationError` → `validate_intent`→`_reject_action(reason)` on `IntentValidationError` → `apply_action`+commit → stamp `Action`→`emit("action", …, room=room:{id})`; ack `{ok, actionId, seq}`), `register_handlers` builds one sequencer + binds `action`. NO DB/schema change → no migration. FRONTEND mirror: `realtime/connection.ts` +`emitAction(socket, payload)→ActionAck` (stamps `ACTION_PROTOCOL_VERSION`) + `ActionAck` iface + `createBoardSocket({onAction})` surfacing broadcast `Action`s (transport primitives; UI wiring deferred to the optimistic-move task). TESTER: backend `test_realtime.py` (+8: join stashes identity, action rejected when not joined / malformed version → error ack no broadcast, host move broadcasts to room + persists Token row, player damage persists clamped to 0, player CANNOT move foreign token (rejected, row unchanged), seq monotonic across two actions, RoomSequencer unit per-room); `register` bind-set tests now include `action`. frontend `connection.test.ts` (+2: onAction surfaces broadcast Action, emitAction stamps version + resolves ack). REVIEWER: identity server-supplied (saved resolved invite, never client) ✓, malformed/unauthorized never broadcast ✓, apply-before-broadcast reconnect-safe ✓, generic reject reasons preserved ✓. DoD: backend ruff+format+mypy --strict clean (43 files), pytest 117 passed (+8); frontend oxlint clean, prettier clean, tsc+vite build clean, vitest 82 passed (+2). · next: Phase 4 "Optimistic move on client + reconcile to server broadcast" (frontend; consumes emitAction/onAction, CLAUDE.md rule 5).
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 4 "Server validates intents (permissions + bounds) before broadcasting" DONE. ARCHITECT sign-off (touches permissions → gated): (1) SCOPE BOUNDARY — this task ships the pure isolated validation GATE only (`app/services/actions.py::validate_intent`); the Socket.IO `action` handler + stamping + broadcast are the NEXT task; Phase-7 "server-enforced permissions" is the broader enforcement this gate composes. (2) NO premature live mutable BoardState store — validate against the durable Token ROWS (same source `build_board_state` reads), consistent with the established "read rows, no in-memory store yet" decision; the mutable store + `seq` belong with apply/broadcast. (3) PERMISSION MODEL (CLAUDE.md rule 3): token-targeting payloads (move/damage) → token must exist AND be in the actor's room; host targets ANY token in its room, player ONLY the token bound to its own character (player w/ character_id None → rejected); non-token payloads (mark/endTurn) allowed for any room participant (whose-turn enforcement deferred to Phase-5 initiative). (4) BOUNDS SPLIT: version/coord/amount/unknown-type rejected at PARSE time by `ActionIntent` (prior task); this gate adds state-aware bounds (token-in-room). IMPLEMENTER: new `app/services/actions.py` — `validate_intent(session, *, room_id, role, character_id, intent) -> ActionPayload` (identity comes from the server-resolved invite, never the client), raises `IntentValidationError(reason)`; `_authorize_token_target` does the room+ownership check; rejection messages GENERIC (no token-enumeration oracle: "not on this board" / "your own token"). No new wire contract → NO frontend mirror needed; no DB/schema change → no migration. TESTER: new `test_action_validation.py` (+11, seeded in-mem DB w/ 2 players + a 2nd room: host moves/damages any token, player moves/damages OWN token, player CANNOT move/damage another player's token, player w/ no character rejected, unknown token → "not on this board", cross-room token rejected even for host, player may mark, any participant may endTurn). REVIEWER: identity server-supplied ✓, generic messages ✓, cross-room blocked ✓, pure unit-tested isolated fn ✓. DoD: backend ruff+format+mypy --strict clean (42 files), pytest 109 passed (+11). · next: Phase 4 "Broadcast resulting Action to all participants in the room" — Socket.IO `action` handler (parse→validate_intent→stamp Action{id,room_id,actor_participant_id,seq}→broadcast to room:{roomId}); lands the live mutable BoardState store + per-room seq + per-sid resolved-identity memory; route via ARCHITECT for the per-sid session/seq/mutable-state shape.
- [2026-06-29] orchestrator(+architect/implementer/tester) — Phase 4 "Action protocol (Pydantic): move, mark, damage, endTurn… defined + versioned" DONE. PURE-schema task (no auth/invite-security/data-deletion surface → no human gate). ARCHITECT decision on the versioned envelope shape: (1) new `app/schemas/action.py` keeps the Action protocol separate from room CRUD contracts; (2) versioning via a single `ACTION_PROTOCOL_VERSION=1` constant + a `version` field whose validator REJECTS unsupported versions → one clean server refusal point when clients drift (bump constant + widen check on incompatible change); (3) per-action payloads are a Pydantic DISCRIMINATED UNION on a `type` Literal (`ActionType` StrEnum: move/mark/damage/endTurn) via `Field(discriminator="type")` → new types are additive + unknown/mismatched types fail to parse; (4) TWO envelopes encode CLAUDE.md rule 1 (server authoritative): `ActionIntent` (client→server) carries ONLY `{version, payload}` — deliberately NO actor/room/id so a client can't spoof who/where it acts; the server stamps those. `Action` (server→all broadcast) = same `payload` + server metadata `{id, room_id, actor_participant_id, seq (per-room monotonic, ge0)}`. Payloads: MovePayload(token_id, x/y bounded by room.GRID_COORD_MIN/MAX), MarkPayload(x,y, color?≤32, label?≤60), DamagePayload(token_id, amount 1..1000 = DAMAGE_AMOUNT_MIN/MAX, capped at the max_hp ceiling), EndTurnPayload(no data). Explicitly NO live mutable BoardState store, NO validation-against-state, NO broadcast/wiring — those are the next two Phase-4 tasks. NO DB/schema change → no migration. FRONTEND contract mirror (`types.ts`): `ACTION_PROTOCOL_VERSION` const, `ActionType` union, Move/Mark/Damage/EndTurnPayload, `ActionPayload` discriminated union, `ActionIntent`/`Action` interfaces. Tests: backend new `test_actions.py` (+13: version const, move-intent narrows via discriminator, mark optional defaults, damage+endTurn member selection, unknown type rejected, parametrized damage-amount bounds 0/-1/1001, parametrized move-coord bounds, mark label cap, intent rejects bad version, Action json round-trip w/ server metadata, Action rejects bad version, seq≥0); frontend new `api/types.test.ts` (+3: version const, discriminated-union narrowing, minimal intent + full broadcast — compile-checked by tsc + runtime asserts). DoD: backend ruff+format+mypy --strict clean (40 files), pytest 98 passed (+15); frontend oxlint clean, prettier clean, tsc+vite build clean, vitest 80 passed (+3). · next: Phase 4 "Server validates intents (permissions + bounds) before broadcasting" — route via ARCHITECT (live mutable BoardState store shape + intent-validation vs Phase-7 server-enforced-perms boundary; touches permissions).
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 4 "Join room → server sends FULL current BoardState (reconnect-safe)" DONE. ARCHITECT sign-off (touches invite-link AUTH → gated): the join handshake gains identity binding by REUSING the already-approved Phase-1 invite resolution — NO new auth primitive invented, NOT Phase-7 per-action enforcement. Decisions: (1) `join` now carries the plaintext invite **token** (the credential the client already holds), not a client-supplied `roomId`; server hashes→matches `token_hash`→active check (revoked/expired)→uniform failure (no enumeration oracle). The roomId is taken from the RESOLVED link, closing the "join any room by id" hole; identity `{participantId, role, characterId}` falls out of resolution. (2) `BoardState` = a Pydantic snapshot built from the durable source-of-truth rows (`{room_id, tokens[], characters[]}`) — exactly what the frontend hydrate already joins, now delivered in one push as a `boardState` event. Complete idempotent read on every (re)join = reconnect-safe (CLAUDE.md rule 2). (3) NO premature in-memory mutable BoardState store — snapshot is read from rows on join; the live mutable store lands with the Action broadcast/validation tasks. (4) DRY: invite resolution extracted to `app/services/invites.py` (`resolve_active_invite`/`is_invite_active` + frozen `ResolvedInvite`); HTTP `api/invites.py` refactored to use it (behavior unchanged, tests still green) so HTTP + realtime authenticate identically. BACKEND: new `app/services/{__init__,invites,board}.py` (board: `build_board_state(session, room_id)`); `schemas/room.py` +`BoardState`; `realtime/events.py` rewritten — token-based `handle_join` (injectable `session_factory` defaulting to `async_session_factory` since Socket.IO handlers can't use `Depends`), `_extract_token`, `_reject_join`, emits `boardState`, ack adds participant/role/character; connect/disconnect unchanged. NO schema/DB change → NO migration. FRONTEND contract mirror: `types.ts` +`BoardState`; `realtime/connection.ts` rewritten — `createBoardSocket(token, {url?, onBoardState?})` joins with `{token}` on every connect (reconnect-safe) + wires `boardState` event; `joinRoom(socket, token)`; `JoinAck` extended. NOT yet wired into a screen (UI wiring belongs to a later Phase-4/5 task). Tests: backend `test_realtime.py` rewritten (token-based, seeded in-memory DB fixture: player join enters room+pushes full snapshot, host join→no character, token trim, unknown→reject, revoked→reject, missing-token parametrized reject; transport/mount tests kept), new `test_services.py` (+8: resolve binding/unknown/blank/revoked/expired, naive-expiry-as-UTC, board empty/populated/room-isolation); `test_invites_api.py` unchanged + still green. frontend `connection.test.ts` rewritten (token join-on-connect, re-join on reconnect, boardState surfaced, joinRoom token emit+ack). DoD: backend ruff+format+mypy --strict clean (43 files), pytest 83 passed (+10); frontend oxlint clean, prettier clean, tsc+vite build clean, vitest 77 passed (+1). · next: Phase 4 "Action protocol (Pydantic): move, mark, damage, endTurn… defined + versioned" — route via ARCHITECT for the versioned envelope shape.
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 4 "python-socketio mounted on FastAPI; client connects via socket.io-client" DONE (Phase 4 STARTED). ARCHITECT decision (cross-cutting transport; NO auth/invite-security/data-deletion surface introduced — connect/join are unauthenticated transport plumbing, identity binding explicitly deferred, so no human gate): (1) MOUNT = one ASGI app wraps both → `app.main:app` is now `socketio.ASGIApp(AsyncServer(async_mode="asgi"), other_asgi_app=create_app(), socketio_path="socket.io")`; `create_app()` still returns the bare FastAPI app (now also exported as `fastapi_app`) so HTTP-only tests/sub-mounting keep working; non-socket.io requests fall through to FastAPI (verified `/health` still 200 through the wrapper). (2) ROOM MODEL = default namespace `/` + Socket.IO rooms named `room:{roomId}` (prefix avoids future name clashes); a client emits `join {roomId}` → server `enter_room` + `joined` ack. (3) SCOPE held tight to the task: connect accepts, disconnect is a no-op (Socket.IO auto-leaves rooms), NO BoardState/Action-protocol/validation/broadcast yet (those are the next 4 Phase-4 tasks). (4) SECURITY: Socket.IO CORS locked to an allowlist (`config.SOCKETIO_CORS_ORIGINS`, comma-sep env, defaults APP_BASE_URL + Vite localhost, deduped) since the SPA connects cross-origin. BACKEND: new `app/realtime/` — `events.py` (testable module fns `handle_connect`/`handle_disconnect`/`handle_join` taking `sio` explicitly + `register_handlers` binding via `sio.on(name, handler)` to dodge mypy `disallow_untyped_decorators` on the untyped lib; `room_name`/`_extract_room_id` reject blank/non-str roomId → emits `error` + failure ack), `server.py` (`create_sio_server`/`create_asgi_app`), `__init__.py` re-exports. main.py wires `app = create_asgi_app(fastapi_app)`. Dep `python-socketio>=5.11` (5.16.3) added; mypy override `socketio.*` ignore_missing_imports (no py.typed); config `SOCKETIO_CORS_ORIGINS` + `_split_origins` helper. FRONTEND: dep `socket.io-client@^4.8.1` (public registry — artifactory unreachable, consistent w/ prior cycles); new `src/realtime/connection.ts` — `createBoardSocket(roomId,url?)` (`io(url,{path:'/socket.io',transports:[ws,polling]})`, re-emits `join` on every `connect` = reconnect-safe), `joinRoom(socket,roomId)→Promise<JoinAck>` (emit w/ ack cb), `SOCKET_BASE_URL` from `VITE_API_BASE_URL`. NOT yet wired into a screen (BoardState-on-join task owns UI wiring). Tests: backend `test_realtime.py` (+16: room_name, connect accepts, disconnect noop, join enters+acks, join trims, 6 parametrized bad-roomId rejects, register binds connect/disconnect/join on real AsyncServer + on plain mock, create_sio_server type, combined ASGI serves `/health` passthrough, combined ASGI serves `/socket.io/?EIO=4` handshake `0...sid`); frontend `connection.test.ts` (+3: io called w/ path + join-on-connect, re-join on reconnect, joinRoom emit+ack-resolve; socket.io-client mocked). DoD: backend ruff+format+mypy --strict clean (34 files), pytest 73 passed (+16); frontend oxlint clean, prettier clean, tsc+vite build clean, vitest 76 passed (+3). · next: Phase 4 "Join room → server sends FULL current BoardState (reconnect-safe)" — route via ARCHITECT first (BoardState shape, live in-memory ↔ persisted Token/Character rows, join auth via invite token).
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 3 "Token rendering: name, HP bar, current conditions" DONE → Phase 3 COMPLETE. ARCHITECT decision (data-fetching/cross-cutting, NO auth/invite-security/data-deletion surface so no human gate): the board needs character display data per token; rather than N+1 per-token `getCharacter` calls, add ONE list endpoint and join client-side. BACKEND: new `GET /rooms/{id}/characters` → `list[CharacterResponse]` (`api/rooms.py`, placed above the existing single-character route so FastAPI matches the literal path first; 404 if room unknown; idempotent reconnect-safe board-hydrate read; no DB/schema change → no migration). FRONTEND contract mirror: `listCharacters(roomId)` client fn (client.ts). Render pipeline kept PURE-math-separate-from-Konva (established board pattern): new `board/tokens.ts` — `tokenRect(token,grid)` (world rect aligned to grid cellSize+offset, size→span, non-positive size→1 cell), `joinTokens(tokens,characters)` (Map by character_id, DROPS dangling tokens with no character), `hpFraction` (clamp [0,1], max_hp≤0→0), `hpBarColor` (green>0.5 / amber>0.25 / red); new `board/TokenLayer.tsx` (react-konva Group/Rect/Text per placed token, WORLD coords inside the stage so pan/zoom apply: footprint Rect + name label w/ bg + HP track+fill bar pinned to footprint bottom + "cur/max" text + conditions line only when present; listening=false, perfectDrawEnabled=false). `MapBoard.tsx` wired: new board-hydrate `useEffect` Promise.all(listTokens,listCharacters)→joinTokens→state, cancelled-flag stale-guard + catch→[] (reconnect-safe), `<TokenLayer>` rendered in the Layer above GridLayer. Tests: backend `test_rooms_api.py` (+3: list returns only this room's chars / cross-room isolation, empty room→[], unknown room→404); frontend `tokens.test.ts` (+8 pure units), `TokenLayer.test.tsx` (+4: one Group/token, name+HP text, conditions joined, conditions line omitted when none, empty→nothing; konva mocked to DOM), `client.test.ts` (+1: listCharacters GET url/reconnect read), `MapBoard.test.tsx` updated (react-konva mock +Group/Rect/Text, mock `../api/client` listTokens/listCharacters→[] + mapImageUrl, tests async w/ act-flush to keep the hydrate state update inside act → quiet console). DoD: backend ruff+format+mypy --strict clean (30 files), pytest 57 passed (+3); frontend oxlint --deny clean, prettier clean, tsc+vite build clean, vitest 73/73 passed (+13). · next: Phase 4 "python-socketio mounted on FastAPI; client connects via socket.io-client" — first realtime/transport task, route via ARCHITECT first (ASGI mount + room model + live-BoardState↔persisted-rows relationship).
- [2026-06-29] orchestrator(+implementer/tester/reviewer) — Phase 3 "Player view: opening a player link shows board + only their character panel" DONE. Scope decision (no architect gate — read-only game data, no auth/invite-security/data-deletion surface): the player panel needed character details but NO character-fetch endpoint existed, so added one following the established no-auth-gate convention (the link is the credential; server-enforced perms remain Phase 7). BACKEND: new `GET /rooms/{room_id}/characters/{character_id}` (`api/rooms.py`) → `CharacterResponse{id,room_id,name,max_hp,current_hp,portrait_url,ability_scores:AbilityScores,conditions:list[str]}` (`schemas/room.py`, from_attributes; nested AbilityScores validates the stored JSON dict, filling 10-defaults); idempotent reconnect-safe read; **404 if unknown OR character.room_id≠room_id** (cross-room guard). No DB/schema change → no migration. FRONTEND: TS `CharacterResponse` mirror (types.ts) + `getCharacter(roomId,characterId)` client fn; new `CharacterPanel.tsx` (self-fetching like MapBoard: loading/loaded/error; renders portrait, name, HP bar w/ role=progressbar, six abilities in STR/DEX/CON/INT/WIS/CHA order with signed modifiers, conditions or "None"); pure `abilityModifier.ts` helper (floor((s-10)/2) signed — split into its own file to keep oxlint fast-refresh rule clean; full rules math deferred to Phase 6 engine); `JoinScreen.tsx` resolved state rewritten → `.board-view` flex layout: player = `<MapBoard>` + `<CharacterPanel>` (ONLY their character); host = `<MapBoard>` + DM panel (no single-character panel). CSS `.board-view`/`.character-panel*` appended to App.css. Tests: backend `test_rooms_api.py` (+4: stat-block happy path, omitted-scores→all-10 defaults + null portrait, unknown→404, cross-room→404); frontend `client.test.ts` (+2: getCharacter GET url/reconnect read, 404→ApiError w/ status), new `CharacterPanel.test.tsx` (+5: abilityModifier units, fetch+render abilities/HP/portrait/condition asserting own-character URL, "None" when no conditions, 404→error), `JoinScreen.test.tsx` rewritten (player→board+panel bound to resolved ids; host→board+DM panel, NO character-panel — both via mocked MapBoard/CharacterPanel), `App.test.tsx` (mock `./board/MapBoard` so App routing test doesn't load konva's node build → needs `canvas`). DoD: backend ruff+format+mypy --strict clean (30 files), pytest 54 passed (+4); frontend oxlint --deny-warnings clean, prettier clean, tsc+vite build clean, vitest 60/60 passed (+6). · next: Phase 3 "Token rendering: name, HP bar, current conditions" (last Phase 3 task; board-side, composes listTokens + grid coords + per-token character display data).
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 3 "Place tokens on the grid; bind each token to a character" DONE. ARCHITECT decision (first live-BoardState piece, cross-cutting data model — no auth/invite-security/data-deletion surface so no human gate): PERSIST a `Token` table as the durable server-authoritative source of truth that the in-memory live BoardState (Phase 4) hydrates from on connect and Phase 7 snapshots build on (CLAUDE.md keeps live board in memory, but a persisted row is the cleanest testable foundation now & matches the existing SQLAlchemy/Alembic pattern). MODEL `app/models/token.py` — Token{id, room_id FK→rooms CASCADE indexed, character_id FK→characters CASCADE **UNIQUE** indexed (one token per character = the binding), x:int, y:int, size:int default 1 (grid cells; 1=Medium/2=Large/3=Huge/4=Gargantuan), timestamps}; registered in models/__init__, Room.tokens + Character.token relationships (delete-orphan). SCHEMAS (`schemas/room.py`): `PlaceTokenRequest{character_id, x/y ge0 le9999 default0, size ge1 le8 default1}`, `UpdateTokenRequest{x?/y?/size?}`, `TokenResponse{id,room_id,character_id,x,y,size}` (from_attributes); constants GRID_COORD_MIN/MAX, TOKEN_SIZE_MIN/MAX/DEFAULT. ENDPOINTS (`api/rooms.py`, host actions by convention — no auth gate yet, same pattern as add_player/map; server-enforced perms remain Phase 7): `POST /rooms/{id}/tokens` 201 (404 room, 404 char, **422 char belongs to another room** = cross-room guard, **409 char already has a token** → reposition via PATCH), `GET /rooms/{id}/tokens` (reconnect-safe idempotent read = board hydrate; 404 room), `PATCH /rooms/{id}/tokens/{tid}` (partial; 422 empty body, 404 wrong-room token). NO DELETE (avoids data-deletion guardrail; removing tokens deferred). Bounds validated server-side (non-negative + sane caps; full grid-dim bounds need client grid config, deferred to Phase 4). Alembic `e3f4a5600003_add_tokens_table` (create_table + 2 indexes incl. unique character_id; single head verified). FRONTEND contract mirror (CLAUDE.md): types.ts +PlaceTokenRequest/UpdateTokenRequest/TokenResponse; client.ts +placeToken/listTokens/updateToken (NO UI yet — board placement/rendering UI belongs to the dedicated Phase 3 "Token rendering" + "Player view" tasks). Tests: backend `test_tokens_api.py` (+13: bound-place happy path, defaults 0/0/1, 409 second token, 404 unknown room/char, 422 foreign char, 422 out-of-bounds neg/oversize, list returns all + reconnect-safe, list 404, patch move+resize partial, patch empty→422, patch wrong-room→404); frontend `api/client.test.ts` (+4: placeToken POST body/url, listTokens GET reconnect-hydrate, updateToken PATCH partial body, 409→ApiError w/ status). DoD: backend ruff+format+mypy --strict clean (30 files), pytest 50 passed; alembic upgrade→downgrade round-trip OK; frontend oxlint+prettier clean, tsc+vite build clean, vitest 54/54 passed. · next: Phase 3 "Player view: opening a player link shows board + only their character panel" (frontend; composes resolve + MapBoard + listTokens).
- [2026-06-29] orchestrator(+implementer/tester/reviewer) — Phase 3 "Character config UI (host): name, portrait, stats, max HP" DONE. Scope decision (no architect gate — no auth/invite-security/data-deletion surface; ability_scores JSON column already existed; portrait modeled as a plain URL string, NOT a new upload/storage subsystem, so reconnect-safe + storage-free). BACKEND (backward-compatible contract extension): `Character.portrait_url` nullable String(2048) + Alembic migration `d2e3f4500002` (batch add/drop, round-trips on sqlite); new `AbilityScores` Pydantic model (strength/dexterity/constitution/intelligence/wisdom/charisma, each ge=1 le=30 default 10 — D&D 2024 range); `AddPlayerRequest` extended with `ability_scores` (default_factory) + `portrait_url` (max 2048, field_validator normalizes blank→None and REJECTS non-http(s) schemes, e.g. blocks `javascript:` injection → 422); `add_player` persists both. Tests `test_rooms_api.py` (+5): scores+portrait persisted, omitted→all 10/no portrait (legacy contract intact), score>30→422, non-http portrait→422, blank portrait→NULL. FRONTEND: TS mirrors `AbilityScores`/`AddPlayerRequest`/`AddPlayerResponse` (types.ts), `addPlayer(roomId,payload)` client fn, new `CharacterConfigScreen.tsx` (host form: name + max HP + six ability inputs defaulting 10 + optional portrait URL + optional player name → POST participants → one-time invite link w/ copy + "add another"); new route `/rooms/:roomId/characters` via `parseRoomCharactersRoomId` wired in App.tsx; CreateRoomScreen success now links to it. CSS `.ability-grid` (3-col) appended. Tests (+8): router (4), App route (1), CharacterConfigScreen (3 — disable-until-valid, POST body asserts ability defaults+portrait, server-error keeps form usable). DoD: backend ruff+format+mypy --strict clean, pytest 38 passed; frontend oxlint+prettier clean, tsc+vite build clean, vitest 50/50 passed; alembic upgrade→downgrade round-trip OK. · next: Phase 3 "Place tokens on the grid; bind each token to a character" — wants ARCHITECT input on Token/BoardState shape (first live BoardState piece).
- [2026-06-29] orchestrator — Phase 2 "Board viewport syncs nothing yet — purely local rendering" DONE → Phase 2 COMPLETE. Confirm-and-tick verification task (no code change needed): the board was designed purely-local from the start. Verified via grep that NO socket/websocket/broadcast/emit/sync code exists anywhere in `frontend/src/board/` or `frontend/src/api/`; MapBoard.tsx carries an explicit scope comment ("PURELY LOCAL rendering — the viewport syncs nothing to the server yet"). Frontend-only, no auth/security/data-deletion surface → no subagent needed beyond direct verification. DoD: vitest 42/42 passed, suite unchanged. · next: Phase 3 "Character config UI (host): name, portrait, stats, max HP" — likely a `pm` spec pass for D&D 2024 stat-block fields first.
- [2026-06-29] orchestrator(+implementer/reviewer) — Phase 2 "Grid overlay (square grid first), configurable cell size + offset" DONE. Frontend-only, no auth/security/data-deletion surface → no architect needed. Followed the established board pattern of isolating PURE math from Konva/React for unit-testability: new `src/board/grid.ts` — `GridConfig{cellSize,offsetX,offsetY}`, `DEFAULT_GRID` (70px cells, no offset), `MIN_CELL_SIZE` 5; `normalizeOffset(offset,cellSize)` wraps any offset (incl. negative / non-finite → 0) into [0,cellSize) via `((o%c)+c)%c`; `buildGridLines(config,width,height)` emits flat `[x1,y1,x2,y2]` world-coord verticals+horizontals spanning [0,width]×[0,height], returns [] for cellSize<MIN (also rejects NaN/Infinity via `!(c>=MIN)`) or non-positive box. New `src/board/GridLayer.tsx` — react-konva `<Line>` per grid line, drawn in WORLD coords INSIDE the existing Stage/Layer so the `viewport.ts` transform pans/zooms it for free; strokeWidth = 1/scale to stay ~1px on screen at any zoom; lines `listening={false}` + `perfectDrawEnabled={false}`. Wired into `MapBoard.tsx`: grid rendered above the KonvaImage in the same Layer (gated on `showGrid`), plus an absolutely-positioned `.map-board__grid-controls` overlay (checkbox toggle + number inputs for cell/X/Y, cell clamped to ≥MIN_CELL_SIZE). Styles appended to App.css. Still PURELY LOCAL (honors ROADMAP "viewport syncs nothing yet"). Tests (Vitest, +9): `grid.test.ts` (6 — offset wrap incl. negative/non-finite, line count + spanning coords, wrapped-offset positions, sub-min/NaN cell → [], degenerate box → []), `GridLayer.test.tsx` (3 — one Line per grid line, strokeWidth=1/scale, empty box renders nothing; react-konva `Line` mocked to DOM so no canvas needed). Updated `MapBoard.test.tsx` mock to add `Line` (MapBoard now renders GridLayer). DoD: oxlint clean, prettier --check clean, `tsc -b && vite build` clean, vitest 42/42 passed. · next: Phase 2 "Board viewport syncs nothing yet — purely local rendering" (likely confirm-and-tick — current design already satisfies it).
- [2026-06-29] orchestrator(+implementer/reviewer) — Phase 2 "Frontend: render map on a Konva stage; pan + zoom" DONE. Frontend-only, no architect needed (no auth/security/data-deletion surface). Added deps konva@^9 + react-konva@^19 (fetched via public npm registry — private artifactory unreachable from sandbox, consistent with prior cycles). New `src/board/`: (1) `viewport.ts` — PURE pan/zoom math kept free of React/Konva so it's unit-testable: `Viewport{scale,x,y}` stage transform, `clampScale` (MIN 0.1/MAX 8, NaN→MIN), `screenToWorld`, `zoomAtPoint` (wheel zoom centred on cursor — keeps world point under pointer fixed; -deltaY zooms in; ZOOM_STEP 1.05; clamped), `fitViewport` (centre + downscale-only initial framing); (2) `useImageElement.ts` — hook loading an HTMLImageElement imperatively, tracks loading/loaded/error, ignores stale loads on url change (reconnect-safe: same idempotent GET reloads cleanly); (3) `MapBoard.tsx` — react-konva Stage/Layer/Image, stage carries the transform, draggable=pan + onWheel=zoom, ResizeObserver-measured container (`useElementSize`, degrades when RO undefined), fits map on load; shows loading/no-map overlays. PURELY LOCAL — viewport syncs nothing yet (honors ROADMAP "Board viewport syncs nothing yet"). Client helper `mapImageUrl(roomId)` added to `src/api/client.ts` (returns absolute `GET /rooms/{id}/map` URL, not fetched here — used as image src). Styles `.map-board`/`.map-board__overlay` appended to App.css. Tests (Vitest, +18): `viewport.test.ts` (11 — clamp bounds/NaN, screenToWorld inverse, zoom in/out + cursor-anchor invariant + max/min clamp, fit downscale/no-upscale/degenerate), `useImageElement.test.ts` (4 — loading→loaded w/ dims, →error, null-url→error, via stubbed global Image), `MapBoard.test.tsx` (3 — loading/error overlays + loaded stage renders KonvaImage at natural size; react-konva + useImageElement mocked so no canvas needed in jsdom). MapBoard not yet wired into a screen (no board route exists until player-view task, Phase 3). DoD: oxlint clean, prettier --check clean, `tsc -b && vite build` clean, vitest 33/33 passed (quiet console). · next: Phase 2 "Grid overlay (square grid first), configurable cell size + offset".
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 2 "API: host uploads a map image; stored + served" DONE. ARCHITECT decision (storage/serving + host-only): (1) STORAGE = local filesystem at `MAP_STORAGE_DIR` (default `./var/map_uploads`), swappable for object storage later behind the same contract; files written under a SERVER-GENERATED `{uuid4.hex}{ext}` name (ext from a content-type allowlist) so the client filename never touches the path → no traversal surface; (2) DATA MODEL = nullable `map_image_path`+`map_content_type` columns on `rooms` (a room has ≤1 current map); re-upload overwrites the pointer and leaves the old file on disk (cleanup deferred — never delete user content implicitly); (3) VALIDATION = content-type allowlist png/jpeg/webp/gif (else 415), 10 MiB cap read via bounded `file.read(cap+1)` (else 413), empty body → 422; (4) PERMISSION = host action BY CONVENTION, consistent w/ existing `add_player`/`revoke` (no auth gate yet — link is the credential); real server-enforced perms remain the explicit Phase 7 task, so no ad-hoc auth invented here and the auth/security guardrail isn't tripped; (5) RECONNECT-SAFE: `GET` is a plain idempotent read. New: `app/storage/maps.py` (lazy `config.MAP_STORAGE_DIR` read so tests/runtime can override; `save_map_image`/`map_image_path`), config keys `MAP_STORAGE_DIR`/`MAX_MAP_UPLOAD_BYTES`/`MAP_CONTENT_TYPE_EXTENSIONS`, `MapResponse` schema, Room columns, endpoints in `app/api/rooms.py` (`POST .../map` 201 multipart UploadFile + `GET .../map` FileResponse; router reads cap/allowlist via `config.` dynamically so monkeypatch works; used non-deprecated `HTTP_413_CONTENT_TOO_LARGE`), Alembic migration `c1d2e3f40001` (batch add/drop columns, round-trips on sqlite), dep `python-multipart>=0.0.9`, `.gitignore` += `var/`. Tests `tests/test_maps_api.py` (+6, temp `MAP_STORAGE_DIR` via monkeypatch + in-mem sqlite): upload→serve byte-exact w/ content-type + persisted server-gen filename≠client name, 415 non-image (nothing stored), 413 oversized (cap monkeypatched to 8), 404 unknown room, 404 no-map, re-upload swaps pointer. DoD: ruff check+format clean, mypy --strict clean (28 files), pytest 33 passed (no warnings), alembic upgrade→downgrade round-trip OK. · next: Phase 2 "Frontend: render map on a Konva stage; pan + zoom" (frontend-only, consumes GET /rooms/{id}/map).
- [2026-06-29] orchestrator(+pm/implementer/reviewer) — Phase 1 "Frontend: host create-room screen; join screen via link" DONE → Phase 1 COMPLETE. UI-only task wiring already-shipped endpoints; no auth/security surface touched, so no architect sign-off needed. New frontend files: `src/api/types.ts` (TS mirrors of Pydantic room/invite contracts — ParticipantRole/RoomStatus, CreateRoom{Request,Response}, RoomSummary, InviteLink, ResolveInviteResponse), `src/api/client.ts` (thin fetch wrapper; `API_BASE_URL` from `VITE_API_BASE_URL` default `http://localhost:8000`; `ApiError` carries HTTP status; `createRoom`/`resolveInvite`), `src/router.tsx` (dependency-free `parseInviteToken` for `/join/:token` matching backend `build_invite_url` shape — no router lib added), `src/screens/CreateRoomScreen.tsx` (room-name required + optional DM name; POST /rooms; shows one-time host link with copy-to-clipboard; server-error surfaced via role=alert; form stays usable), `src/screens/JoinScreen.tsx` (resolves token on mount via GET /invites/{token}; loading→resolved/invalid/error states; uniform friendly "invalid or has expired" on 404 honoring the server's no-enumeration 404; reconnect-safe read). `App.tsx` rewritten to route on injectable `pathname` (default create-room). Styles appended to App.css. Tests (Vitest, offline via stubbed global fetch + fireEvent — no new deps; user-event NOT added since not installed): router (6), App routing (2), CreateRoomScreen (3: disabled-until-name, POST+host-link happy path asserting body+url, server-error), JoinScreen (4: player binding, host/DM message, 404 invalid, network error). DoD: oxlint clean, prettier --check clean, `tsc -b && vite build` clean, vitest 15/15 passed. · next: Phase 2 "API: host uploads a map image; stored + served" — wants ARCHITECT input on storage/serving + host-only permission.
- [2026-06-29] orchestrator(+architect/implementer/reviewer) — Phase 1 "Link security: unguessable tokens, single-purpose, revocable" DONE. ARCHITECT sign-off (auth/invite-security, this cycle): (1) unguessable (secrets.token_urlsafe 256-bit) + hash-only storage + resolve-side revoke/expiry enforcement already existed; the missing piece was the WRITE side of "revocable" → a host revoke endpoint; (2) revoke is addressed by `(room_id, participant_id)`, NEVER by the secret plaintext token, so a host can invalidate a *player's* link without knowing the player's secret (consistent with no-auth-gate-yet design where the link is the credential); (3) RECONNECT-SAFE (CLAUDE.md rule 2): resolve stays read-only/idempotent — a destructive one-time `used_at` consumption would break link reloads, so it was NOT added; "single-purpose" is satisfied by the existing invariants (each link binds exactly one room/participant/character; tokens never reused, new row per mint; `used_at` left reserved/non-rejecting). New endpoint `POST /rooms/{room_id}/participants/{participant_id}/revoke` (`app/api/rooms.py`): sets `revoked_at=now()` on all currently-active links for the participant (revoked_at IS NULL filter), idempotent (2nd call revokes 0), returns `RevokeLinksResponse{revoked:int}`; 404 if participant missing OR participant.room_id != room_id (cross-room guard). Contract `RevokeLinksResponse` added to `app/schemas/room.py`. Tests `tests/test_rooms_api.py` (+4): revoke disables link + breaks resolve (uniform 404) + persists revoked_at; idempotent (1 then 0); unknown participant→404; cross-room participant id→404 and link stays active. DoD: ruff check+format clean, mypy --strict clean (25 files), pytest 27 passed (+4). Phase 1 backend complete. · next: "Frontend: host create-room screen + join screen via link" (closes Phase 1).
- [2026-06-29] orchestrator(+architect/implementer/reviewer) — Phase 1 "API: resolve invite link → {roomId,participantId,role,characterId}" DONE. ARCHITECT sign-off (auth/invite-security, this cycle): (1) endpoint `GET /invites/{token}` — resolution is an idempotent read; token-in-path matches the existing `/join/{token}` design; (2) lookup by hashing presented token → unique `token_hash` (O(1)); (3) SECURITY: unknown / revoked / expired links ALL return a uniform 404 "Invalid or expired invite link." — no enumeration oracle, honors `revoked_at`+`expires_at` already in schema; (4) RECONNECT-SAFE (CLAUDE.md rule 2): resolve is read-only and does NOT set `used_at`, so reloads can always re-resolve — one-time consumption deferred to the "Link security" task. New router `app/api/invites.py` (`_is_active()` helper: revoked_at IS NULL AND (expires_at IS NULL OR now<expires_at), normalizes tz-naive expires_at→UTC), included in `create_app` (main.py). Contract `ResolveInviteResponse{room_id,participant_id,role,character_id|None}` added to `app/schemas/room.py` (character_id None for host). Tests `tests/test_invites_api.py` (+7): host→no character, player→bound character, idempotent+used_at stays None, unknown→404, revoked→404, expired→404, future-expiry→200. DoD: ruff check+format clean, mypy --strict clean (25 files), pytest 23 passed (+7). No DB writes on resolve. · next: "Link security" task (revoke endpoint + optional used_at policy) — wants ARCHITECT sign-off.
- [2026-06-29] orchestrator(+implementer/reviewer) — Phase 1 "API: generate per-participant unique invite links bound to a character slot" DONE. New endpoint `POST /rooms/{room_id}/participants` (201): host action that creates a new `Character` slot (name + max_hp, `current_hp` seeded = max_hp), a player `Participant` (role=player) bound to that character via `character_id`, and a fresh `InviteLink` — reusing the SAME architect-signed-off security scheme as the host link: `generate_token()` plaintext shown once, only `hash_token()` SHA-256 persisted, never plaintext. Returns `{participant_id, character_id, role, invite_link{token,url}}`. 404 if room missing (no character/link rows created — verified). Contracts added to `app/schemas/room.py`: `AddPlayerRequest` (character_name 1–120; max_hp gt0/le1000; optional display_name≤120) + `AddPlayerResponse` (reuses `InviteLinkResponse`, url=`{APP_BASE_URL}/join/{token}`). Router extended `app/api/rooms.py` (added `session.get(Room)` lookup + `HTTPException(404)`, imports Character/uuid). Tests: `tests/test_rooms_api.py` +4 — link-bound-to-character happy path (asserts char slot fields, participant.character_id binding, token_hash==sha256(token)≠plaintext), unique tokens+distinct char/participant ids across two players in one room, unknown-room→404 (no rows), max_hp=0→422. DoD: ruff check+format clean, mypy --strict clean (23 files), pytest 16 passed (4 new). No resolve/revoke enforcement here (creation only) — that's the next task + the "Link security" task, which want ARCHITECT sign-off. · next: API resolve invite link (route via ARCHITECT first for resolve + link-security).
- [2026-06-29] orchestrator(+implementer/reviewer) — Phase 1 "create room (host) → room id + host link" DONE. `POST /rooms` (201): inserts Room(status=lobby) + host Participant(role=host, no character) + InviteLink, returns `{room{id,name,status}, host_participant_id, host_role, host_link{token,url}}`. Implements the architect-signed-off invite-link security (no new sign-off needed — design was already approved into the schema): new pure util `app/security/tokens.py` — `generate_token()` = `secrets.token_urlsafe(32)` (256-bit, unguessable) shown once; `hash_token()` = SHA-256 hex; **only the hash is persisted**, never plaintext (test asserts `token_hash == sha256(token)` and `!= token`). Added Pydantic contracts `app/schemas/room.py` (CreateRoomRequest validates name 1–120 chars; CreateRoom/RoomSummary/InviteLinkResponse), router `app/api/rooms.py` (Annotated `get_session` dep, flush→commit), wired via `include_router` in `create_app`. New config `APP_BASE_URL` (default Vite `http://localhost:5173`, no trailing slash) builds `{base}/join/{token}`. Tests: `test_rooms_api.py` (3: full happy-path+persistence+hash invariant, unique tokens across rooms, blank-name→422 via dependency-overridden in-memory sqlite ASGI client) + `test_tokens.py` (3: 1000-token no-collision/url-safe, sha256 determinism, hash≠plaintext). DoD: ruff check+format clean, mypy --strict clean (23 files), pytest all green (6 new). No auth GATE yet — host link is the credential; resolve/revoke enforcement deferred to the explicit "Link security" task (wants architect sign-off). · next: API generate per-participant invite links bound to a character slot.
- [2026-06-29] orchestrator(+architect/implementer/tester/reviewer) — Phase 1 data model landed. ARCHITECT sign-off on invite-link security baked into the schema: InviteLink stores only a SHA-256 `token_hash` (unique+indexed, never plaintext), plus `revoked_at` (revocable), `expires_at`+`used_at` (single-purpose/time-bound); reissue = revoke old row + insert new (uniqueness on token_hash, not participant). Added SQLAlchemy 2.0 async models — `app/models/{room,participant,character,invite_link,enums}.py` on a shared `app/db/base.py` (DeclarativeBase + TimestampMixin), `app/db/session.py` (async engine + `get_session` dep), `app/config.py` (DATABASE_URL env, sqlite default for offline/test). Wired Alembic (async `env.py` reading app.config, `alembic.ini`, mako template) + autogenerated initial migration `b6c7e100aa86`; verified `alembic upgrade head` → `downgrade base` round-trips on sqlite. Deps added: sqlalchemy[asyncio]/asyncpg/alembic (+aiosqlite dev). Added `backend/.gitignore` (*.db). Tests: `tests/test_models.py` (6) cover full graph+relationships, JSON defaults, **token_hash UNIQUE** invariant, link-active defaults. DoD: ruff check+format clean, mypy --strict clean (15 files), pytest 6 passed. FK cascades on room delete are model-level only (no deletion ops added). · next: API create-room (host) → room id + host link.
- [2026-06-29] orchestrator — Added `.github/workflows/ci.yml`: two parallel jobs on push(master/main)+PR+manual, with concurrency cancel-in-progress. Backend job (astral-sh/setup-uv@v5 cache, py3.12, `uv sync --frozen`) runs `ruff check`, `ruff format --check`, `mypy`, `pytest -q`. Frontend job (setup-node@v4, node 22, npm cache, `npm ci`) runs oxlint, prettier `format:check`, vitest, build. Verified all 8 commands green locally; `uv sync --frozen` OK (35 pkgs); both lockfiles present; YAML valid (PyYAML `on`→bool quirk is harmless for GH Actions). Phase 0 DONE · next: Phase 1 Room/Participant/InviteLink/Character data model — invite-link security needs ARCHITECT sign-off first.
- [2026-06-29] orchestrator — Lint/format wired both packages. Backend: added `ruff>=0.8` + `mypy>=1.13` dev deps and `[tool.ruff]` (line-length 100, py312, select E/W/F/I/UP/B/C4/SIM/RUF) + `[tool.mypy]` (strict, files app+tests, relaxed decorators for tests) to pyproject; `uv sync`; ruff autofixed one import-sort in test_health.py → `ruff check`/`ruff format --check` clean, `mypy` strict clean, pytest 2 passed. Frontend: added `prettier@^3` dev dep + `.prettierrc.json` (singleQuote, semi, trailingComma=all, printWidth 100) + `.prettierignore`; `format`/`format:check` scripts; ran `npm run format` → `format:check` clean, oxlint clean, build clean, vitest 1 passed. Decision: kept oxlint (ESLint-family, already wired & green) as the linter instead of adding ESLint to avoid duplicate/conflicting linters; prettier covers formatting. npm install needed public registry (sandbox-gated) · next: CI script (lint+tests both packages).
- [2026-06-29] implementer — Added root `docker-compose.yml` (postgres:16 default w/ named volume `dnd_pgdata` + pg_isready healthcheck; redis:7 gated behind `profiles: [cache]` w/ healthcheck) and root `.env.example` (POSTGRES_*, asyncpg `DATABASE_URL`, `REDIS_URL`; `.env` already gitignored). No app/DB code touched. Validated YAML structure + env vars via uv python (postgres has no profile so it's default-up; redis profile=[cache]; DATABASE_URL uses postgresql+asyncpg://) → ALL OK. `docker compose config` not runnable in sandbox (approval-gated) so used programmatic schema/env validation instead · next: ruff+mypy / eslint+prettier.
- [2026-06-29] implementer — Wired Vitest (jsdom + Testing Library + jest-dom); added `src/App.test.tsx` smoke test + `src/setupTests.ts`; `test`/`test:watch` scripts. Test config lives in standalone `vitest.config.ts` (Vitest's nested Vite plugin types clash with project Vite 8/rolldown, so kept out of `tsc -b`). Deps fetched from public npm registry (private artifactory was unreachable). `npm run test` → 1 passed, `npm run build` clean, `npm run lint` clean · next: docker-compose Postgres + `.env.example`.
- [2026-06-29] orchestrator — Verified pytest + pytest-asyncio wired (`asyncio_mode=auto`, dev deps fastapi/httpx); `uv run pytest -q` → 2 passed (incl. /health ASGITransport smoke test). DoD met, ticked · next: frontend Vite boot + Vitest smoke test.
- [2026-06-29] orchestrator — Verified Backend uv project + FastAPI `/health` + `scripts/run.sh` (executable); `uv run pytest` → 2 passed. DoD met, ticked · next: pytest smoke-test task (likely already green) then frontend Vitest smoke.
- [2026-06-29] implementer — Scaffolded monorepo: restored `/backend` (FastAPI app factory, `/health`, pytest) and `/frontend` (Vite+React+TS) from known-good history; `uv run pytest` → 2 passed · next: backend uv project + `/health` + run script task.
- [INIT] host — Repo initialized with Claude Code agent setup. Begin Phase 0.
